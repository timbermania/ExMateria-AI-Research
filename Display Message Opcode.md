# Display Message Opcode

The event instruction `{10}` Display Message is FFT's text/dialogue opcode: a 1-byte opcode + fixed parameter block (Dialog selector, 1-based string-table Message index, Unit, Portrait row, X/Y/Arrow-X offsets, Open Type). The Dialog byte's high nibble picks the box type (3-line portrait boxes, Check/Help boxes, the no-box screen overlay, …) and its low nibble the vertical alignment plus additive arrow flags; Message indexes the per-event string table that follows the `0xDB Event End`, encoded in the FFT charmap with inline control codes such as `0xE2 NN` = `{Delay NN}`. The open/typewriter handler is `FUN_801308c0` with dialog state around `0x80166000`–`0x801661FF`, and glyphs are read one byte at a time by `event_text_glyph_reader` (`0x8014CE80`). Boxed dialog advances on CIRCLE (`0x8012F6D4`) and closes via a later `{51} Change Dialog`, while the screen-overlay (prayer) path is not input-gated, and box-type-0 overlays self-dismiss via a self-timed brightness fade on typewriter completion. The `Dialog=0x09` overlay and the boxed portrait variants are implemented and tested in the Godot reimplementation (boxed portraits = EVTFACE override when a `{50}` row is active, else the speaker's unit-SPR face — see [[Event Dialogue Portrait System]]). The scenario-8 chapter-opening narration (`Dialog=0x0B`, box-type-0, valign Center) was captured dynamically on 2026-07-10: `Msg=1` is a multi-page narration (≥3 pages, cleared between) with each page centered at PSX row `116 − 8·n_lines` (pitch 16), and the PC-36 `Task=1` barrier holds until the last page clears, deferring the PC-38 `{33}` Color Field. Godot renders 0x0B through the generalized box-type-nibble test (`(dialog & 0x70) == 0`) with per-page centering and page-break pagination; the boxed portrait variants are implemented (the EVTFACE row/column system, see [[Event Dialogue Portrait System]]). A 2026-07-02 re-decode established that the X/Y/Arrow-X placement fields are hand-authored per line (non-zero live-captured), pinned the order-of-ops with the clamps interacting, renamed the Dialog `0x80` bit `CHAIN_CONTINUE` (the "close" name was inverted), identified the `0x20`/`0x30` "side-markers" as pre-rendered "Check"/"Help" text labels, and decoded the message-data control bytes (`0xEC`/`0xFD`/`0xFE`/`0xFF`).

## Points

- **Opcode `{10}` is a 1-byte opcode followed by a fixed parameter block in stream order — Unknown (1B, the "always seems to be x10" / catalog-default 0x16 programming-error constant the engine ignores), Dialog (1B, box-type/layout selector), Message (2B LE u16, 1-based string-table index), Unit (2B, unit whose portrait + position the box attaches to), Portrait (1B, portrait row; `0x09` removes the portrait and its space), X (2B signed), Y (2B signed), Arrow X (2B signed), Open Type (1B) — and our disassembler decodes exactly this layout (scenario-1 prayer: `Dialog=0x09, Message=0x01, Unit=0x0C, X=0, Y=0x3C, Open Type=0` at chunk offset `0x100`, PC=42).** — `[S·R] 2/3`
  - S: FFTPatcher `EventCommands.xml` parameter block (opcode table); scenario-1 event-chunk decode at offset `0x100` (PC=42)
  - R: `godot-learning/tools/disasm_event.py` decodes exactly this layout
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **The Dialog byte's high nibble selects the box type — x0X centered screen overlay (x00 do-not-use), x1X/x7X/x9X 3-line dialog box (includes portrait; x9X closed with `{51}`), x2X/xAX 1–4 line "Check" (xAX supports pages), x3X/xBX 1–4 line "Help" (xBX supports pages), x4X 2-line, x5X/xDX 8-line (xDX no portrait, supports pages), x6X 1–14 line without paging, x8X no box — text over the screen, center-aligned, slow draw, closed with `{51}`, xCX 1–2 line no portrait, supports pages, xEX 1–8 line without paging (≥9 lines won't appear, and `WaitForInstruction(x01)` with ≥9 lines hangs the game), xFX = x1X — and its low nibble selects vertical alignment (1 = top, 2 = bottom, 3 = center; center puts the portrait box over the unit's head with the arrow flipped horizontally) plus additive arrow flags (4 = thinking bubbles, 8 = remove arrow, doesn't work with x9X); if the game doesn't detect a (sufficiently close) `{51}` afterward to close the box it may not display the message at all.** — `[S·R] 2/3`
  - S: ffhacktics "Event Instruction 10" box-type table, corroborated by the scenario-1 chunk decode — across all 18 `{10}` instances `Dialog=0x09` (overlay family, no speaker header) is unique, and every boxed instance is `0x11/0x12/0x91/0x92` with a `{Color 08}Speaker{Newline}{Color 00}` header and a non-zero Open Type
  - R: `godot-learning/tools/disasm_event.py` decoded all 18 scenario-1 instances
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **The Message half-word is a 1-based index into the per-event string table, which begins immediately after the `0xDB Event End` opcode that terminates the event bytecode (scenario 1: chunk `+0x8F9`, RAM `0x8004B005+`); there is no Message `0x0000`, and while the full u16 could point at arbitrary RAM lines the wiki notes that is pointless.** — `[S] 1/3`
  - S: scenario-1 string-table location — chunk `+0x8F9`, RAM `0x8004B005+` (per `SCENARIO_LOADING.md` §3.1)
  - S: FFTPatcher `EventCommands.xml` — {DB} Event End (no params; terminates the bytecode, string table follows), {E3} Event End 2
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **Event strings are FFT-charmap encoded with inline control codes — `{Delay NN}` (pacing) is encoded as `0xE2 NN` in the byte stream (byte `0xE2` is also a standalone opcode in the bytecode region; context disambiguates), plus `{Newline}`, `{Color NN}` (00 = body, 08 = speaker-name highlight), name macros such as `{Ramza}`, and `{DA xx}` space/word-load helpers (e.g. `{DA73}` adds an invisible side space to fix `{EB}`-loaded word clipping in x8X mode).** — `[S·R] 2/3`
  - S: byte-level encoding in the scenario-1 event string table (RAM `0x8004B005+`)
  - R: `godot-learning/tools/decode_fft_text.py` + `godot-learning/tools/data/psx_charmap.json` resolve these tokens; validated by `godot-learning/tools/test_decode_fft_text.py`
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **Each visible glyph is read one byte at a time by `event_text_glyph_reader` at `0x8014CE80` (caller `0x8013284C`), which dispatches the FFT charmap render and is shared by the box and overlay text paths.** — `[S] 1/3`
  - S: `0x8014CE80` (`event_text_glyph_reader`), caller `0x8013284C`
  - S: byte scanner (masks `0xFE`, matches the `0xE0`-family escapes), per `research/working_documents/TYPEWRITER_TEXT_CADENCE.md` §2.B
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **The Unit parameter is only meaningful for portrait box types: if a unit with the matching Unit ID is on the map, the box centers over/under it and shows that unit's portrait; otherwise a blank portrait is shown (still occupying the portrait space) and the box renders on the left of the screen; `Portrait 0x09` removes the portrait and reclaims its space; the sign of X and Arrow X govern portrait facing (any negative value mirrors the portrait to the other side — set either to −1 to flip in place), and odd X/Y values may rattle the box.** — `[ ] 0/3`
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **Open Type is an additive opening-animation/style bitfield: x01 = +50% open speed, x02 = −50% open speed and remove bounce, x04 = darken the dialog box, x10 = toggle arrow (points left if FALSE, right if TRUE); "bounce" is the box growing past its real size (~110%) then settling back to 100%.** — `[ ] 0/3`
  - ⚠ SUPERSEDED (2026-08-19) by: the arrow operand byte msg+0x64 (instr [14]) is not an additive bitfield — its low nibble (& 0xf) indexes the 5-entry box-open keyframe tween table (0 overshoot/bounce, 1 big overshoot, 2 smooth linear, 3 short ease-in, 4 4-step, also driving the typewriter text-reveal fade) and its high nibble (& 0xf0) is a non-zero-vs-zero gate for the tail ±16px nudge + L/R mirror
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **Box type x8X (screen overlay, "prayer" mode) behaves unlike the boxed types: no box — text is drawn directly over the screen, center-aligned, and drawn more slowly (`{Delay 00}` speeds it up, but you need at least one char at `{Delay 02}` or the 6th line briefly glitches); only one can be active at a time, so a `{E5} WaitForInstruction(x01,x00)` must separate two of them; `{EB}` word-loading can clip letter ends, mitigated inconsistently by `{DA73}` padding.** — `[S] 1/3`
  - S: scenario-1 chunk decode — the prayer at chunk offset `0x100` (`Dialog=0x09`, the x0X/overlay family) exhibits the same no-box, raw-body, inline-`{Delay}` character
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **The open-and-typewriter handler for `{10}` is `FUN_801308c0` (static-confirmed 2026-06-27); it seeds the dialog-state globals around `0x80166000`–`0x801661FF`, and the per-frame text tick advances the typewriter cursor while consuming `{Delay NN}` (`0xE2 NN`) tokens.** — `[S·D] 2/3`
  - S: `FUN_801308c0`; dialog-state globals `0x80166000`–`0x801661FF` (static-confirmed 2026-06-27, per `display_message_overlay_decode.md` §C)
  - S: FFTPatcher `EventCommands.xml` master catalog row {10} — `FUN_801308c0` evt0x10 handler
  - D: round-2 BP (`probe_display_message_v2.py`, `orbonne_prayer_pre_scenario_load.sstate`, 2026-06-27): `0x801308c0` + `FUN_80132824` measurer JAL fire exactly once each in the 35-second window (the single Display Message in it); top-level dispatcher `0x8013e904` / `BATTLE_block_start_event_instruction 0x8013e8e8` show 0 hits — queue-then-tick routing
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **A single global text-speed throttle is written by `FUN_8013da00` (7 callers); the living doc associates it with a "Set Text Speed" opcode, but the catalog has `0x76 = Dark Screen`, so the opcode-number attribution is pending reconciliation.** — `[S] 1/3`
  - S: `FUN_8013da00` (7 callers in the disassembly)
  - S: FFTPatcher `EventCommands.xml` {76} row — "Dark Screen" (Unknown:1, Shape:1, Screen Expansion Speed:1, Rotation Speed:2, Square Expansion Speed:1); master catalog keeps the disasm cross-ref provisional pending static confirmation of the 0x76 dispatcher case
  - ⚠ SUPERSEDED (2026-08-17) by: the main-executor comparison chain pins `0x75` → `set_event_text_glyph_throttle` (`0x8013da00`) and `0x76` → DarkScreen (`battle:0x80145260`) — the catalog's provisional `0x76 → 0x8013da00` cross-ref was a misattribution
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **Input advance is gated for boxed dialog only: `event_dialogue_tick` at `0x8012F6D4` polls the pad via `SCUS_get_inverted_button_input` (`0x8012FBF8`), tests the CIRCLE bit (`andi v0,0x20` at `0x8012FC00`), and writes the advance flag to `0x80166080`; the screen-overlay/prayer path is not input-gated — in scenario 1 the next opcode is `{F1} Wait` (86 frames), so it auto-advances.** — `[S·D] 2/3`
  - S: `0x8012F6D4` (`event_dialogue_tick`), `0x8012FBF8` (`SCUS_get_inverted_button_input`), CIRCLE-bit test at `0x8012FC00`, advance flag `0x80166080`
  - D: live Exec-BP — tick fired 343× (~60 Hz while D held, 0 hits in pure idle) and `0x80166080` held the advance-action code 1/2/8 per pressed button (2026-06-20, per `SCENARIO_LOADING.md` Q#2/§2.5)
  - ⚠ SUPERSEDED (2026-08-19) by: boxed dialog advances on CROSS (X), not Circle — the `andi 0x20 @ 0x8012FC00` poll is the Cross bit of the PSX pad byte; live `setOverride(14)`/Cross advanced the chapel boxes and `setOverride(13)`/Circle did nothing
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **Page-able (and `*`-marked) box types close on a later `{51} Change Dialog`; the scenario-1 screen overlay (`Dialog=0x09`) has no explicit close opcode — the text simply persists until overwritten or the scene moves on (exact clear trigger is open question A.3 in the living doc).** — `[S] 1/3`
  - S: scenario-1 chunk decode — no `{51}` follows the overlay prayer
  - ⚠ SUPERSEDED (2026-08-19) by: box-type-0 overlays do not persist until overwrite — on typewriter completion the render path runs a self-timed brightness fade intrinsic to it (seed 0x80 @ 0x80131624, −8/frame @ 0x801316A4, floor 0x38, written to +0x114/+0x115/+0x116), no input or downstream opcode required; boxed dialogs take the input-gated Cross-advance path instead
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **The `Dialog=0x09` screen overlay (Orbonne chapel prayer, "God, please help us sinful children of Ivalice…") is implemented in the Godot reimplementation — `DialogueOverlay.gd`, wired through `ScenarioVM`, rendered from the real BATTLE.BIN font atlas with the typewriter + `{Delay}`/`{Color}`/`{Newline}` token handling.** — `[R] 1/3`
  - R: `godot-learning/src/scenarios/DialogueOverlay.gd` wired via `godot-learning/src/scenarios/ScenarioVM.gd`; validated by `godot-learning/tests/DialogueOverlayTest.gd`
  - src: `research/wiki_articles/event_instruction_10_display_message.md`
- **The scenario-8 chapter-opening narration at PC 35 (`{10}` Display Message, chunk offset 198) decodes to `Dialog=0x0B, Msg=1` with 59 tokens ("Delita's name appears…") — box type 0 (no box), valign 3 (Center), Y=0 — and versus the working Orbonne prayer `0x09` the only material decode differences are valign Top→Center (`local_ce` 1→3) plus Y 60→0; palette, cadence, non-blocking, and Task=1-hold are identical.** — `[S·R] 2/3`
  - S: handler `FUN_801308c0`; box-type/valign taxonomy in `scenario_1_captures/display_message_dialog_type_and_palette_decode.md` Part 3; PC35 params in `godot-learning/assets/scenarios/chunks/scenario_008_chunk.json`
  - R: `godot-learning/src/scenarios/ScenarioDecode.gd` `display_message` (`is_overlay = (dialog & 0x70) == 0`, commit `fa88b19b`) + `DialogueOverlay.gd` valign dispatch, validated by `godot-learning/tests/DialogueOverlayTest.gd` `_test_valign_center_placement`
  - src: `research/working_documents/HANDOFF_scenario8_display_message_pc35.md`
- **`Msg=1` (the 59-token scenario-8 narration) is a multi-page message on the PSX — at least three pages that are cleared between pages; the overlay pages forward rather than rendering all 59 tokens as one block or scrolling.** — `[D·R] 2/3`
  - D: dynamic scenario-8 display-message overlay capture, Part Z of `scenario_1_captures/display_message_overlay_decode.md` (2026-07-10)
  - R: `godot-learning/src/scenarios/DialogueOverlay.gd` `_split_pages` / `_PAGE_BREAK_MIN_DELAY` page-break logic, validated by `godot-learning/tests/DialogueOverlayTest.gd` `_test_split_pages_and_content_lines`
  - src: `research/working_documents/HANDOFF_scenario8_display_message_pc35.md`
- **With valign Center, each page is vertically centered: the first line's top sits at PSX row `first_line_top_psx = 116 − 8·n_lines` with line pitch 16 — the Center-Y the box-type-0 RE had never captured, now measured.** — `[D·R] 2/3`
  - D: dynamic scenario-8 display-message overlay capture, Part Z of `scenario_1_captures/display_message_overlay_decode.md` (2026-07-10)
  - R: `godot-learning/src/scenarios/DialogueOverlay.gd` `_first_line_psx_y` (`_CENTER_ANCHOR_PSX=116`, `_LINE_PITCH_PSX=16`, top-margin clamp), validated by `godot-learning/tests/DialogueOverlayTest.gd` `_test_valign_center_placement`
  - src: `research/working_documents/HANDOFF_scenario8_display_message_pc35.md`
- **The PSX holds the PC-36 `{E5} Wait For Instruction Task=1` dialog barrier for the *entire* multi-page narration — the scene RGB is rock-stable while the text is up, and the PC-38 `{33}` Color Field never dispatches until the text has cleared.** — `[D·R] 2/3`
  - D: dynamic scenario-8 display-message overlay capture, Part Z of `scenario_1_captures/display_message_overlay_decode.md` (2026-07-10)
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` `TASK_DIALOG` liveness (`_register_task_kinds` — holds while `dialogue_overlay.is_active()` or the dialog gate is active), validated by `godot-learning/tests/ScenarioWaitForInstructionTest.gd` (Task=1 dialog-gate cycles) + `godot-learning/tests/DialogueOverlayTest.gd` `_test_multipage_advance_holds_active`
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` `_hold_wait_until` (line 1641) progress-aware hold: `ctx.wait_until_progress` extends the deadline on progress (`wait_until_last_progress` to `wait_until_deadline_tick = _vm_tick + wait_until_watchdog_ticks`), holding the Task=1 past the fixed 1800-tick backstop — validated by `godot-learning/tests/ScenarioWaitForInstructionTest.gd` (cycles 4b/4c)
  - src: `research/working_documents/HANDOFF_scenario8_display_message_pc35.md`
- **Godot now renders `Dialog=0x0B` as a screen overlay instead of skipping it as unsupported: the overlay-vs-box decision generalized from the prayer-specific `dialog == 0x09` to the box-type nibble `(dialog & 0x70) == 0` (boxed variants are `0x1X/0x7X/0x9X`), so box-type-0 messages all take the no-box overlay path.** — `[S·R] 2/3`
  - S: box-type-nibble split RE-proven — handler `FUN_801308c0`, `scenario_1_captures/display_message_dialog_type_and_palette_decode.md` Part 3
  - R: `godot-learning/src/scenarios/ScenarioDecode.gd` `display_message` (commit `fa88b19b`), validated by `godot-learning/tests/DialogueOverlayTest.gd` (0x0B overlay centering, pagination, self-dismiss) + `godot-learning/tests/ScenarioWaitForInstructionTest.gd`
  - src: `research/working_documents/HANDOFF_scenario8_display_message_pc35.md`
- **The scenario-1 dialogue string table decodes verbatim under the FFT charmap — single-byte ASCII alphanumerics + `!` (`0x00–0x09` digits, `0x0A–0x23` A–Z, `0x24–0x3D` a–z, `0x3E` `!`), control bytes `0xFA` space / `0xF8` soft line break within a page / `0xFE` end of page (next speaker name follows), and 2-byte extended punctuation `0xD9 B6` `.`, `0xDA 74` `,`, `0xD9 C1` `'`, `0xD9 C9` `?`; in this dump page text begins with `0xE3 0x30` and speaker names with `0xE3 0x38` (the charmap labels the 0xE3 NN family `{Color NN}` — the doc reads them as page/speaker boundary markers), pages follow the `{NEXT_SPEAKER_NAME}{NP}{PAGE_TEXT_LINES}{NL}` structure, and the speaker name between the `{E3}8` highlight tag and the next `{NP}` names the NEXT page's speaker.** — `[S·D·R] 3/3`
  - S: FFTPatcher `PSXText.xml` + `CharMap.cs` encoding table (per doc §3.1)
  - D: live RAM decode of the scenario-1 cinematic dialogue verbatim (Ovelia, Agrias, Priest, Gafgarion, Simon, Black Knight) (2026-06-20, `scenario_1_captures/file_load_capture.json` + `dialogue_pages_decoded.txt`)
  - R: `godot-learning/tools/data/psx_charmap.json` (0xFA→space, 0xF8→`{Newline}`, 0xE2 NN→`{Delay NN}`, 0xE3 NN→`{Color NN}`, the four punctuation pairs above) + `godot-learning/tools/decode_fft_text.py` (0xFE/0xFF string terminators, 2-byte lead bytes) — validated by `godot-learning/tools/test_decode_fft_text.py`
  - src: `research/working_documents/SCENARIO_LOADING.md`
- **`FUN_801308c0` decodes the Dialog byte into four masked fields at `0x80130980..0x8013099c` — `& 0x70` box type (`local_d0`), `& 0x3` vertical align (`local_ce`: 1=Top, 2=Bottom, 3=Center, 0=auto-over-unit), `& 0xc` arrow flags (`local_cc`: 4=thinking bubble, 8=remove arrow), `& 0x80` the `0x9X` close/pageable bit (`local_ca`) — and `0x7X` is remapped into the `0x10` 3-line portrait box (`(d2 & 0x70)==0x70 → d2 = (d2 & 0x1c) | 0x10 | (portrait_idx+1)`), so `0x1X/0x7X/0x9X` all render as box type `0x10`.** — `[S·R] 2/3`
  - S: mask decode `0x80130984`/`0x8013098c`/`0x80130994`/`0x8013099c` + the 0x7X remap @ ~`0x80130930` in `FUN_801308c0` (`battle_decompilation.c`, static round 1 2026-06-27)
  - R: `godot-learning/src/scenarios/ScenarioDecode.gd` `display_message` (boxed-vs-overlay split on `Dialog & 0x70`, `is_overlay = (dialog & 0x70) == 0`) + `src/scenarios/ScenarioVM.gd` `_op_display_message` — validated by `godot-learning/tests/ScenarioBoxedDialogTest.gd` `_test_routing_predicate`
  - ⚠ SUPERSEDED (2026-08-19) by: the Dialog byte & 0x80 bit is CHAIN_CONTINUE, not "close" (the name was inverted) — bit clear: {0xFE} closes the box and {0xFD} scrolls in-buffer; bit set: both {0xFD} (@ 0x80131868) and {0xFE} (@ 0x80131a2c) auto-advance to the chained next message (FUN_8014cbf4 = msg+0x04), re-layout, continue; it also gates the uniform-width precompute (msg+0x68)
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **The 3-line portrait box auto-sizes to its text: height = `lines*16 + 16` (line height 16 px, +16 chrome; thinking-bubble flag adds +8/+16), width = text width + `0x18` with +`0x40` (64 px) reserved when a portrait is present (`Portrait < 8`), and the box type forces the line count (`0x00`→6, `0x10`→3 forced, `0x20/0x30` cap 4, `0x40` cap 2, `0x50`→8).** — `[S·D·R] 3/3`
  - S: sizing math in `FUN_801308c0` — width `text+0x18` @ `0x80130b34`, `text+0x40` @ `0x80130b1c`, line pitch @ `0x80130b50` (`battle_decompilation.c`)
  - S: per-kind line-count clamp table `0x80130a30`–`0x80130ae0` — `0x00`→6 @ `0x80130a4c`, `0x10`→exactly 3 @ `0x80130a60` (`ori v0,zero,0x3`), `0x50`→8 @ `0x80130a3c`, `0x20`/`0x30` clamp ≥5 to 4 @ `0x80130a74`/`0x80130aa4` (`slti`), `0x40` clamp ≥3 to 2 @ `0x80130ad0`; line count measured by `FUN_8013018c` (`0xF8` `{Newline}` scan @ `0x8013026c`, lines = newline count + 1) (battle_disassembly.txt)
  - D: live capture `last_run/probe_boxed_dialog.jsonl` (2026-06-27): compositor widths 156/170/162 across the three chapel boxes = text + padding
  - R: `godot-learning/src/ui3/assemblies/DialogueBox.gd` auto-sizing (text + 56 px portrait dock, the ROM's 0x40 reservation trimmed) — validated by `godot-learning/tests/DialogueBoxTest.gd` `_test_box_geometry`
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
  - src: `research/working_documents/scenario_1_captures/dialogue_pagination_and_page_icon_decode.md`
- **The box is anchored on the speaking unit's projected screen position: `DAT_8016e40c`/`DAT_8016e40e` = live unit screen X/Y (projected via `FUN_80133158`/`FUN_80133048`), box X = `unit_x − width/2 − 4` clamped to the screen `[0x88, 0x180−width]`; align 1 (Top) puts the box top at `unit_y − height − 0x28` (triangle on the box's BOTTOM edge, pointing down), align 2 (Bottom) puts the box at `unit_y` (triangle on the TOP edge, pointing up), and align 0 auto-selects from whether headroom remains above the unit.** — `[S·D·R] 3/3`
  - S: staging math in `FUN_801308c0` (projection load @ `0x80130b90`, `battle_decompilation.c`, static round 1 2026-06-27)
  - D: live capture (2026-06-27, `probe_boxed_dialog.jsonl`): anchor BP `s0 = 0x8016e40c`, unit screen (235,160)/(196,142)/(207,121) changing per speaker
  - R: `godot-learning/src/scenarios/DialogueBoxPlacement.gd` `solve` (`ALIGN1_TOP_OFF = −0x28`, center-under-unit + edge clamp) — validated by `godot-learning/tests/DialogueBoxPlacementTest.gd` (`_test_edge_clamp_shove_flips_portrait`, `_test_arrow_y_align_and_bubble`)
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **For box types NOT anchored on a unit (the `LAB_80130f20` fallthrough), the box sits in fixed screen slots from the low nibble: Top → `y=0x16`, Bottom → `y=0xe4−height`, Center → `y=(0x78−height/2)&~1`, with X centered at `(0x100−width/2)&~1` — a box working area of X∈[`0x88`,`0x180`], Y∈[`0x16`,`0xe4`] (~136..384 × 22..228 px).** — `[S] 1/3`
  - S: `LAB_80130f20` fallthrough in `FUN_801308c0` (`battle_decompilation.c`, static round 1 2026-06-27)
  - R: none — the non-unit-anchored fixed-slot align path is not present in godot-learning (the port always anchors the box to the speaker unit; probed `godot-learning/src/scenarios/`, `godot-learning/src/ui3/`)
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **The speaker triangle is a 16×16 sprite living as TWO stacked cells at FRAME.BIN-atlas X=`0x58` (88) — (88,0) = arrow-UP (box BELOW the unit, drawn on the box's top edge, dst-Y=0) and (88,16) = arrow-DOWN (box ABOVE the unit, bottom edge, dst-Y=`0x38`) — with a horizontal mirror applied for L/R facing.** — `[S·D·R] 3/3`
  - S: triangle blit @ `0x8014c430` (src X set at `0x8014c2f8`), edge Y @ `0x8014c35c`, facing mirror `FUN_8014c014 @ 0x8014c440` (`battle_decompilation.c`, static round 2 2026-06-27)
  - D: live src-rect capture `probe_boxed_dialog.jsonl` (2026-06-27): (88,0,16,16) for the two below-unit boxes, (88,16,16,16) for the above-unit box
  - R: `godot-learning/tools/parse_frame.py` (emits `assets/ui/dialog_triangle_up.tga` / `dialog_triangle_down.tga` + `dialog_triangle.json`) + `src/ui3/assemblies/DialogueBox.gd` triangle quads — validated by `godot-learning/tests/DialogueBoxTest.gd` `_test_arrow_up_down_and_remove`
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **The triangle aims at the mouth: dst-X within the box = `(width/2) + local_b8 − 8` clamped to `[0x10, width−0x10]`, where `local_b8` is the box's accumulated screen-edge clamp shove — so the triangle keeps tracking the unit's true screen X even after the box is shoved inward from a screen edge; `local_b8 < 0` flips facing (portrait + triangle face the other way), and the portrait side follows the same sign (right dock `box_left + width − 0x30` / left dock `box_left + 8`).** — `[S·D·R] 3/3`
  - S: dst-X clamp @ `0x8014c3f4`/`0x8014c3fc`, facing test `bgez` @ `0x8013114c`, portrait side-switch @ `0x80131194` (`battle_decompilation.c`)
  - D: live dst-X 70/52/71 for widths 156/170/162 (2026-06-27 capture) back-solve to `local_b8 ≈ 0/−25/−2` — box 2's negative offset flips facing, consistent with "triangle X = unit screen-X, clamped inside the box"
  - R: `godot-learning/src/scenarios/DialogueBoxPlacement.gd` `solve` (shove accumulator, `TAIL_BOX_MARGIN`, `tri_scr_x = box_left + local_b8 + half_w`, sign-based portrait dock) — validated by `godot-learning/tests/DialogueBoxPlacementTest.gd` `_test_tri_box_x_golden_lines`
  - ⚠ SUPERSEDED (2026-08-19) by: the tail's ±16px shift and L/R mirror are gated on the authored arrow operand msg[0x64] & 0xf0 (non-zero vs zero only, gate @ 0x8014c39c), NOT sign(local_b8) — sign(local_b8) flips only the portrait quad + portrait side
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **The box chrome is NOT GPU-sampled from FRAME.BIN — the frame 9-slice, glyphs, and triangle are software-composited into a RAM buffer (`0x8016eb84`; the compositor's work buffer double-buffers `0x8016a37c`/`0x8016a77c` 0x400 apart) and uploaded to VRAM in ONE GsLoadImage @ `0x8014c45c`; the chrome atlas is a BATTLE.BIN-RESIDENT byte-identical copy of EVENT/FRAME.BIN at `0x8014d5d4` (`hud_font_src_ptr @ 0x80173f5c`, single writer `0x8013eff4`), 9-sliced by `FUN_8014c758` inside the compositor `FUN_8014c18c` (via `FUN_8014c164`).** — `[S·D] 2/3`
  - S: compositor `FUN_8014c18c @ 0x8014c18c` / 9-slice `FUN_8014c758 @ 0x8014c758` / upload GsLoadImage `0x8014c45c` / atlas `0x80173f5c → 0x8014d5d4` (`battle_decompilation.c`, static round 2 2026-06-27)
  - D: GsLoadImage arg capture (a0/a1=`0x8016eb84`, a2=`0x801697b0`, a3=`0x801697c0`) + full-VRAM needle-match control — FRAME.BIN 480/525 needles resident verbatim vs EVTFACE.BIN 0/824 (2026-06-27, `last_run/vram_dump.bin`)
  - R: none — the software RAM-composite + GsLoadImage chrome path is not present in godot-learning (the port draws `UIFrame`/`UIText`/triangle quads from the extracted FRAME.BIN assets instead; probed `godot-learning/src/`, `godot-learning/tools/`)
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **`FUN_801308c0` (size 0x1F64) is the WHOLE opcode-0x10 handler — Dialog decode, sizing, unit→screen projection, primitive setup, the glyph typewriter loop (calling `event_dialogue_tick @ 0x8012f6d4`), and open/close — while `FUN_80132bc4` (size 0x28C) is ONLY the open/close bounce tween that corner-lerps the box + portrait quads from a collapsed origin to the full rect (`FUN_80132e74`/`FUN_80132e50`, 12-bit fixed-point) and fires ~once per message, not the renderer.** — `[S·D] 2/3`
  - S: `FUN_801308c0` size 0x1F64; tween `FUN_80132bc4 @ 0x80132bc4` + corner lerp `FUN_80132e74`/`FUN_80132e50` (`battle_decompilation.c`, static round 2 2026-06-27 — corrects the overlay doc's "FUN_80132bc4 is the boxed renderer")
  - D: exec-BP hit counts (2026-06-27): `FUN_80132bc4` fires only ~once per Display Message; `{51}` swaps do not re-run it
  - R: `godot-learning/src/ui3/assemblies/DialogueBox.gd` open/close tween (curve-driven growth from the triangle pivot) — validated by `godot-learning/tests/DialogueBoxTest.gd` (`_test_open_tween_grows_along_curve3`, `_test_close_tween_shrinks_curve4`)
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **`{51} Change Dialog` services the boxed path by RE-ENTERING the same `FUN_801308c0` handler (the op10 BP fires with `a1=0x51`), re-running the full decode→project→composite→GsLoadImage pipeline into the SAME buffer `0x8016eb84` — an in-place swap of speaker + portrait + text + triangle with the frame persisting, not a teardown (caveat: the leading `0x10` of each box wasn't captured in that mid-conversation run).** — `[D·R] 2/3`
  - D: live press cycle off `orbonne_prayer_mid_dialog.sstate` (2026-06-27, `probe_boxed_dialog.jsonl`): every advance re-fired `FUN_801308c0` with `a1=0x51` into the same buffer; the open/close tween was not re-run
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` `_op_change_dialog` (in-place swap routed through `ScenarioDialogueBoxPool`) — validated by `godot-learning/tests/ScenarioBoxedDialogTest.gd` `_test_message_index_and_swap` + `godot-learning/tests/DialogueBoxTest.gd` `_test_inplace_swap`
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **Boxed dialog advances on CROSS (X), not Circle: in the chapel capture `setOverride(14)`/Cross advanced the box and fired the full op-10 pipeline while `setOverride(13)`/Circle did nothing — the `andi 0x20` button test at `0x8012FC00` is the Cross bit of the PSX pad byte, and the advance flag still lands at `0x80166080`.** — `[S·D·R] 3/3`
  - S: `andi v0,0x20` @ `0x8012FC00` in `event_dialogue_tick @ 0x8012F6D4` (`battle_disassembly.txt`)
  - D: live press cycle off `orbonne_prayer_mid_dialog.sstate` (2026-06-27): Cross (PCSX override bit 14) advances + fires the pipeline; Circle (bit 13) no-op
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` `_dialog_advance_pressed` (project `dialog_advance` action mapped to Cross/confirm, fallback `ui_accept`) — validated by `godot-learning/tests/ScenarioBoxedDialogTest.gd` (`_test_advance_two_press`, `_test_auto_advance_dwell`)
  - ⚠ SUPERSEDED (2026-08-19) by: {E5} Wait For Instruction (Task=1) is a player-advance gate on the foreground (kind-1) box only — held indefinitely until the player advances the box (Circle in the 2026-07-01 capture), not an auto type-out wait and not a box-close wait; background (kind-0x33) boxes do not hold it (see [[Concurrent Dialogue Boxes]])
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **The speaker name is NOT a separate header primitive: it is the `{Color 08}Name{Newline}{Color 00}` prefix typed as line 0 of the box, and the `{Color}` token swaps the glyph CLUT mid-string (`local_d4 → DAT_8016dae2`, into the font decoder `FUN_8014bd88`) — so the header/body two-palette split is one mid-string CLUT swap inside a single text object, not two text objects.** — `[S·R] 2/3`
  - S: mid-string CLUT swap on the shared glyph path (`local_d4 → DAT_8016dae2` → `FUN_8014bd88`), `battle_decompilation.c` (static round 2 2026-06-27)
  - R: `godot-learning/src/scenarios/TypewriterController.gd` `typewriter_set_palette` per-run sink (+ `flatten` per-char palette) — validated by `godot-learning/tests/DialogueBoxTest.gd` `_test_palette_runs_split_header_body`
  - src: `research/working_documents/scenario_1_captures/boxed_dialog_decode.md`
- **When a right-docked portrait is present, `godot-learning`'s `DialogueBox` anchors the pagination icon to the portrait's LEFT edge (`_box_w_px - PORTRAIT_RIGHT_INSET_PX - PAGE_ICON_PORTRAIT_GAP_PX - sz.x`, bottom edge baseline-aligned with the portrait) instead of the box outer bottom-right corner, so the icon never overlaps the face — user verbal spec governs because no PSX reference capture has a portrait + page icon together (the paginating beat has no portrait).** — `[R] 1/3`
  - R: `godot-learning/src/ui3/assemblies/DialogueBox.gd::_layout_page_icon` (left-of-portrait anchor when `_has_portrait and not _portrait_on_left`) + `godot-learning/tests/DialogueBoxTest.gd::_test_page_icon_left_of_right_portrait`
  - src: `research/working_documents/scenario_1_captures/concurrent_dialogue_boxes_decode.md`
- **The `{10}` Display Message is a fixed 15-byte instruction whose placement fields are all hand-authored per line — signed box-X offset [8..9] → `msg+0x58`, signed box-Y [10..11] → `msg+0x5c`, signed triangle-only fine-X [12..13] → `msg+0x60`, arrow operand [14] → `msg+0x64`, computed width override → `msg+0x68` — so "forehead here, way above there" = the operands differing per line, not geometry; the 15-byte length is confirmed by the per-opcode operand-size table `DAT_8014d170` (`[0x10] = 0x0E` = 14 operands + 1 opcode byte, cursor advance `iVar13 += 1 + tbl[op]` @ `LAB_80145f24`), the handler never reads past [14], and the slot is zero-inited at spawn (`FUN_8014c8a0`) so a line that authors nothing leaves them 0 (the degenerate centred case).** — `[S·D·R] 3/3`
  - S: `FUN_80143bd8 @ 0x80143BD8` `if (uVar25 == 0x10)` dispatch block reads each operand from its own instruction bytes (instr = `DAT_80173ca4` + cursor) into `DAT_801698c4`/`0x1698c8`/`0x1698cc`/`0x1698d0`/`0x1698d4`; length table `DAT_8014d170` + advance @ `LAB_80145f24` (`battle_decompilation.c`, per the doc §1a/§1b)
  - D: `scripts/probe_triangle_authored_offsets.py` (attach mode, pcsx :8080; load → arm → CROSS-advance — placement runs once per line, not per frame) captured non-zero authored operands live: X58 ∈ {−31, 0, 20}, Y5c ∈ {−8, −6, 0}, fineX60 ∈ {−32, 0, 1, 12}, arrow64 low-nibble 0/1/2/3, on the Orbonne prayer + Female-Knight lines (2026-07-01)
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` `_op_display_message` (decodes X/Y/Arrow X as signed i16 via `_s16`, threads them `ScenarioDialogueBoxPool._show_dialog_box` → `_place_box_on_unit`) + `src/scenarios/DialogueBoxPlacement.gd` `solve` — validated by `godot-learning/tests/DialogueBoxPlacementTest.gd` (29 golden assertions)
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **The authored offsets apply in a fixed order-of-ops with the clamps interacting: `box_left = projX − w/2 − 4` (+`0xC` align-1 / `−8` align-2), clamped `[0x88, 0x180−w]` (shove accumulates into `local_b8`), THEN `+= X58` with a second clamp; `box_top = (align1: projY−h−0x28 | align2: projY) += Y5c`, clamped `[0x16, 0xe4−h]`; `local_b8 += fineX60`, clamped `[0x18−w/2, w/2−0x18]` before `tri_boxX` — so the tail tracks the unit's column only on all-zero lines, and the authored shoves compound through the clamps rather than adding independently.** — `[S·D·R] 3/3`
  - S: `box_left` base + clamps + X58 @ `0x80130b90..0x80130d88`; `box_top` + Y5c @ `0x80130c88`/`0x80130d24`; fineX60 fold + clamp @ `0x80130d9c`/`0x80130dc0..e14`; `tri_scrX = box_left + local_b8 + w/2` @ `0x80130e34..e68` (inside `FUN_801308c0`, `battle_decompilation.c`, per the doc §1b)
  - D: the §2 probe (2026-07-01) reproduced the rev-1 `tri_boxX` goldens (70/52/71/86/74) while exposing the operands behind them — the all-zero Ovelia-still line reproduces the centred tail, the Female-Knight line `X58=−31 + fineX60=−32` strands the tail well left of the projected unit (hand-placement, not geometry)
  - R: `godot-learning/src/scenarios/DialogueBoxPlacement.gd` `solve` implements the exact project → centre −w/2−4 +align tweak → clamp → += X58 → clamp → box_top(align, += Y5c, clamp) → local_b8 += fineX60 → clamp order — validated by `godot-learning/tests/DialogueBoxPlacementTest.gd` golden lines (p3=70, p4=52 & local_b8=−25, p5=71 & −2, p6=86, fk16=74)
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **The Dialog byte's `& 0x80` bit is `CHAIN_CONTINUE`, not "close" (the earlier name was inverted): bit CLEAR → `{0xFE}` closes the box (`LAB_8013276c`, frees the slot) and `{0xFD}` does an in-buffer scroll (`FUN_801307d4`); bit SET → BOTH `{0xFD}` (@ `0x80131868`) and `{0xFE}` (@ `0x80131a2c`) auto-advance to the chained next message (`FUN_8014cbf4` = `msg+0x04`), re-layout (`FUN_8014c164`), and continue; it also gates the uniform-width precompute (called unconditionally from the `0x10` dispatcher but early-returning inside `FUN_8012f4e0` when clear).** — `[S·D·R] 3/3`
  - S: extracted `local_ca = Dialog&0x80` @ `0x80130998`; consumed in the typewriter loop @ `0x80131868`/`0x80131a2c`; chain fetch `FUN_8014cbf4` (`msg+0x04`) + re-layout `FUN_8014c164`; width gate `if((param_2&0x80)!=0)` inside `FUN_8012f4e0 @ 0x8012F4E0` (`battle_decompilation.c`, per the doc §6.2)
  - D: the §2 live table (2026-07-01) — every close-bit line has `w68 ≠ 0` (92/106/98/122/100); the two non-close lines (msgid 7 `dlg=0x11`, msgid 15 `dlg=0x12`) have `w68 = 0`
  - R: `godot-learning/src/scenarios/ScenarioDialogueBoxPool.gd` `_slot_persists[slot] = (dialog & 0x80) != 0` (per-slot PERSIST bit behind the kind-1 foreground / kind-0x33 background demotion) — validated by `godot-learning/tests/ScenarioBoxedDialogTest.gd` `_test_first_event_non_persist_closes_on_advance`
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **Box types `0x20`/`0x30` "side-markers" are pre-rendered TEXT LABELS, not icons: each is the box primitive group's 4th SPRT (template `FUN_8012e2b8`/`FUN_8012e244` leaves it `w=h=0` ⇒ invisible for every other box-type), and the dedicated branches write a real source rect blitting "Check"/"Help" — `0x30` @ `0x8013126c` src (80,32) → VRAM (1040,288) 20×9 "Help"; `0x20` @ `0x801312c0` src (64,120) → VRAM (1024,376) 28×8 "Check"; both dst `(box_left+3, box_top−2)`, CLUT `0x7cbc`, atlas page `getTPage(0,2,0x3c0,0x100)` = VRAM (960,256) — menu/info-window headers, a different box class from character dialogue (`0x10`-only).** — `[S·D] 2/3`
  - S: dedicated branches `0x8013126c`/`0x801312c0` + the zero-size 4th-SPRT template (`battle_decompilation.c`, per the doc §6.3)
  - D: `scripts/render_boxtype_markers.py` blitted those exact source rects from `last_run/vram_dump_orbonne_prayer_mid_dialog.bin` → `last_run/marker_0x20_*.png` / `marker_0x30_*.png`; the crops read "Check"/"Help" with the surrounding UI-label atlas (`Lv. Time GIL Status Help … Check Effect …`) (2026-07-02)
  - R: none — the 0x20/0x30 marker SPRTs not present in godot-learning (probed `godot-learning/src/`, `godot-learning/tests/`)
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **The message-data control bytes are fully decoded: `0xEC` (1 operand byte) sets the speaker portrait/face expression — writes the portrait index to the render struct (`puVar10[3]`/`[0x49]`) and publishes event-var `0x5a` via `FUN_8013b644`, seeds the initial portrait if a message BEGINS with it, overrides the slot default `*(msg+0xc)−1`; `0xFD` = inline wait-for-button pause with NO page flush (chain bit clear → resume the same buffer in place; set → load next chained message); `0xFE` = end-of-page — pads remaining rows + flushes, then chain bit clear → close box, set → wait then chain to next (reloads portrait); `0xFF` = close immediately, no wait/flush; companions: `0xE0` name, `0xE2` {Delay}, `0xE3` colour, `0xE4`/`0xE6` numbers, `0xF4`/`0xF8` newlines, `0xF9` release-other-slots, `0xFB`/`0xFC` choice menu.** — `[S] 1/3`
  - S: the interpreter is `FUN_801308c0`'s own if/else cascade (not a jump table); the byte-fetch `FUN_801307d4 @ 0x801307D4` also expands the `0xF0–0xF3` dictionary compression (`battle_decompilation.c`, per the doc §6.4)
  - R: none — 0xEC portrait-override / 0xFD-vs-0xFE page semantics not present in godot-learning (probed `godot-learning/src/`, `godot-learning/tests/`; only `0xE2` {Delay} + `0xF8` newline exist, in `tools/psx_charmap.json` + `TypewriterController`)
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **The box-width override `msg+0x68` is a CHAIN-uniform width: `FUN_8012f4e0` is called unconditionally from the `0x10` dispatcher but only computes when the `CHAIN_CONTINUE` bit (`Dialog & 0x80`) is set — it walks the chained `0x51` ('Q') sub-messages sharing the align mask and takes the MAX text width, so a chain of boxes gets one uniform width; at render `if (msg[0x68] != 0) w = msg[0x68]` else auto-measure per message.** — `[S·D] 2/3`
  - S: `FUN_8012f4e0 @ 0x8012F4E0` (internal `param_2 & 0x80` early-return; max-width walk over chained `0x51` sub-messages); width-override read @ `0x80130a14` (`battle_decompilation.c`, per the doc §1c/§6.2)
  - D: the §2 probe (2026-07-01) — `w68 ≠ 0` iff the CHAIN bit is set: close-bit lines carry 92/106/98/122/100, the two non-close lines (msgid 7 `0x11`, msgid 15 `0x12`) carry 0
  - R: none — the `msg+0x68` chain-uniform width override not present in godot-learning (probed `godot-learning/src/`, `godot-learning/tests/`; the port measures each box's own rendered px)
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **The arrow operand byte `msg+0x64` (instr [14]) is not an additive bitfield: the low nibble (`& 0xf`) is the box-open TWEEN selector — `param_4` of `FUN_80132bc4 @ 0x80132BC4`, indexing the 5-entry keyframe table `PTR_DAT_80167908` (`0` overshoot/bounce, `1` big overshoot, `2` smooth linear, `3` short ease-in, `4` 4-step) which also drives the typewriter text-reveal fade (and is forced on the box close/erase path regardless of [14]); the high nibble (`& 0xf0`) is a non-zero-vs-zero gate ONLY — any non-zero value applies the tail ±16px nudge (direction by align) + horizontal mirror (`FUN_8014c014`, gate @ `0x8014c39c`), the specific value is irrelevant.** — `[S·D·R] 3/3`
  - S: `FUN_80132bc4 @ 0x80132BC4` + `PTR_DAT_80167908` (5 keyframe entries); nudge/mirror gate @ `0x8014c39c` inside compositor `FUN_8014c18c` (`battle_decompilation.c`, per the doc §1c)
  - D: the §2 live capture (2026-07-01) — arrow64 low-nibble 0/1/2/3 observed across the chapel lines (four different box-open tweens); every chapel line has `&0xf0 == 0`, so neither the nudge nor the tail mirror ever fired there
  - R: `godot-learning/src/ui3/assemblies/DialogueBox.gd` `_arrow_mirror = (open_type & 0xf0) != 0` (set in `show_dialog`; mirror applied on the triangle scale) + `src/scenarios/DialogueBoxPlacement.gd` ±16px tail nudge gated on `msg[0x64] & 0xf0` — validated by `godot-learning/tests/DialogueBoxTest.gd` `_test_offset_flips_portrait_arrow_gates_triangle` (98/98) + `tests/DialogueBoxPlacementTest.gd` (29/29)
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **On the non-portrait fall-through (`LAB_80130e7c`, entered via `bne local_d0,0x10 @ 0x80130b04` or `beq local_cc,8 @ 0x80130b5c`), `align == 0` is GENUINELY UNDEFINED: the box-Y local is written only for align 1/2/3 (`0x16` / `0xe4−h` / `0x78−h/2`), so align 0 is read uninitialised @ `0x80130ef4` (stack garbage), `+= msg[0x5c]`, and — unlike the portrait path — NOT clamped; the `align==0` auto-resolution (`FUN_8014cc28`/`FUN_80149734`, side pick, `local_ce` set to 2 or 1 if `DAT_8016e40e ≤ DAT_8016e406`, written back into the Dialog byte) exists only on the portrait+tail path.** — `[S] 1/3`
  - S: `LAB_80130e7c` + entry branches @ `0x80130b04`/`0x80130b5c`; uninit read @ `0x80130ef4`; portrait-path auto `FUN_8014cc28`/`FUN_80149734 @ 0x80149734` (`battle_decompilation.c`, per the doc §6.1)
  - R: none — the non-portrait align-0 path not present in godot-learning (probed `godot-learning/src/`, `godot-learning/tests/`; `DialogueBoxPlacement.gd` treats align 0 as align 2 by convention)
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **The message-slot base is `msg = *(0x80165f98) + *(0x80174038) * 0x400` (`PTR_DAT_80165f98 = &DAT_8016986c`; `DAT_80174038` = active slot index, stride `0x400`; slot-0 fields alias the `DAT_801698xx` globals); the `{10}` window/dialog id byte [1] (`FUN_80149bec @ 0x80149BEC`) is a DIRECT message-slot index 0..15 with sentinel `0x10` = auto-pick first free slot — all styling comes from the Dialog byte [2], never from [1].** — `[S] 1/3`
  - S: `FUN_80149bec @ 0x80149BEC` slot select; slot base `PTR_DAT_80165f98`/`DAT_80174038`, stride `0x400` (`battle_decompilation.c`, per the doc §1/§1c)
  - R: none — the 16-slot `0x400`-stride message-slot globals not present in godot-learning (probed `godot-learning/src/`, `godot-learning/tests/`; the port's `ScenarioDialogueBoxPool` keeps a 1-based UI box pool, not ROM message slots)
  - src: `research/working_documents/scenario_1_captures/dialogue_box_triangle_aim_decode.md`
- **The screen-overlay position model is left-anchored within a centered span, not per-glyph re-centering: the block left edge (`local_b4` = ANCHOR − full_message_width/2 − 4, half-width from `local_e4>>1` @ `0x80130b6c`) is computed ONCE before typing starts (`sw` @ `0x80130bac`), and the renderer pins the container there and advances the X cursor rightward as glyphs blit in (`0x80130e5c..0x80130e78`), so shorter lines end up ragged-right within the centered block.** — `[S·R] 2/3`
  - S: `0x80130b6c` (srl s1,1), `0x80130bac` (sw local_b4), `0x80130e5c..0x80130e78` (renderer screen-X write) (`battle_disassembly.txt`, round-5 2026-06-29 re-read)
  - R: `godot-learning/src/scenarios/DialogueOverlay.gd` `_measure_full_block_width` + `_position_label` (container pinned to left edge of the centered block; glyph layout starts at x=0) — validated by `godot-learning/tests/ScenarioPrayerOverlayTest.gd` (mid-frame "God" left edge stable) + `DialogueOverlayTest.gd`
  - src: `research/working_documents/scenario_1_captures/display_message_overlay_decode.md`
- **Box-type-0 (no-box) overlays self-dismiss without input or downstream opcode: on typewriter completion the box-type-0 render path runs a self-timed brightness fade intrinsic to it — seed `s1 = 0x80` @ `0x80131624`, ramp `addiu s1, s1, −8` @ `0x801316A4`, floor guard `slti …, 0x31` (last written value `0x38`), written per-frame to the composed-text primitive brightness at `+0x114/+0x115/+0x116`; final window teardown at the message terminator `0xFF` / task-state==3 (`event_fiber_mark_complete @ 0x801327E8`); boxed dialogs (`local_d0 ≠ 0`) instead take the input-gated Cross-advance path (`event_task_set_kind(0x33)` + button poll) and skip the fade.** — `[S·D·R] 3/3`
  - S: `0x80131624`, `0x801316A4`, `0x801327E8` (BATTLE.BIN static, 2026-07-10 §Z.9)
  - D: pcsx @8087 no-input playthrough (`/tmp/dismiss_capture.py`, `scenario_7_8_start.sstate`, 2026-07-10): last page held bright, then vanishes on its own (`dc_366.png` to `dc_367.png`), before the `{7D}` Show Graphic fades in, with no button press
  - R: `DialogueOverlay._on_page_complete` to `_begin_auto_dismiss` on the LAST page (ROM fade constants `_FADE_START_V=0x80` / `_FADE_STEP_V=8` / `_FADE_FLOOR_V=0x38`); `is_active()` stays true through the fade so the Task=1 barrier holds until the text is actually gone — validated by `DialogueOverlayTest._test_boxtype0_self_dismisses_without_external_clear` (+ updated completion/prayer/multipage cases, 70/0)
  - src: `research/working_documents/scenario_1_captures/display_message_overlay_decode.md`
- **Box-type-0 multi-page pagination closes a page when a `{Delay}` token with frames ≥ 0x3C (60-frame hold) is followed by more text — the hold, then a `{Newline}` run, then typing on a new page; `{Delay 19}` (25) and below never break; this is neither a fixed line-count capacity (page A clears at 3 lines while B reaches 6) nor a blank-line-run rule (B to C breaks on a single `{Newline}`); pages auto-advance on timing with no input (static confirmation of the box-type-0 renderer clear condition still open, non-blocking per §Z.6).** — `[D·R] 2/3`
  - D: no-input playthrough + settled-page captures (`/tmp/play_nomash.py` + `/tmp/page_settled.py`, `scenario_7_8_start.sstate`, pcsx @8087, 2026-07-10; 0.28 s/step sampling resolved all three page clears + the single-page prayer fit)
  - R: `DialogueOverlay._split_pages` (`_PAGE_BREAK_MIN_DELAY = 0x3C`) + leading/trailing blank-strip + per-page re-center — validated by `DialogueOverlayTest._test_split_pages_and_content_lines` (+ `_test_valign_center_placement`, `_test_multipage_advance_holds_active`)
  - src: `research/working_documents/scenario_1_captures/display_message_overlay_decode.md`
- **The whole-game "hacktics" Ghidra export (`project-assets/hacktics_disassembly.txt`) names the boxed-dialog routines that the BATTLE.BIN-only export leaves as `FUN_`: `CreatePortraitPolygon(POLY_FT4*, MiscUnitID)` renders the speaker portrait as a `POLY_FT4` (called from a BATTLE site adjacent to `FUN_80132bc4`), `Get_Portrait` / `Get_Unit_Portrait` fetch the portrait (CODE seg, e.g. `0x8005e09c`), `Get_FRAME_CDROM_to_FrameBuffer` + `FRAMETW1/2/3` @ `0x80045170` load FRAME.BIN into VRAM with its texture windows, and `BATTLE_main_BattleMessageDisplay_state_handler` is the message-display advance/close state machine.** — `[S] 1/3`
  - S: `project-assets/hacktics_disassembly.txt` (whole-game Ghidra export with human-named PSX-SDK/asset/battle symbols) cross-referenced against our `battle_disassembly.txt` addresses (2026-06-27)
  - R: none — the hacktics routine names not present in godot-learning (probed `godot-learning/src/` + `godot-learning/tests/` for `CreatePortraitPolygon`/`Get_Portrait`/`FRAMETW`/`0x8005e09c`/`0x80045170`)
  - src: `research/working_documents/scenario_1_captures/handoff_boxed_dialog_next.md`

## Notes

(empty — user territory)

## Related

- [[Scenario Table]]
- [[Event Opcode Catalog]]
- [[Color Tint Luma Modes]]
- [[Scenario Beat Capture]]
- [[Event Dialogue Portrait System]]
- [[Pad Input Handler]]
- [[Concurrent Dialogue Boxes]]
- [[Dialogue Box Geometry]]
- [[Dialogue Pagination]]
- [[Dialogue Font Palette]]
