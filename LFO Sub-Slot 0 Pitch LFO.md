# LFO Sub-Slot 0 Pitch LFO

FFT's sub-slot 0 pitch-LFO (mode 0, the channel-level LFO on the sub+0xE0 block): when its `active_dir` active bit is set, the mode-0 swap+accumulate path (0x800175A4–0x800175E0) fires every cadence, swaps the accumulator's sign on the countdown wrap, commits the callback return's high-16 into `chan+0x88` (`chan+0x88 += ret >> 16` at PC `0x800175A4`, over the value `per_channel_tick` pre-cleared to 0 each tick), and arms a pitch prestage by writing `chan_word_1 |= 0x200` (`CHAN1_PITCH_PRESTAGE`), which the per-tick drain commits as SPU pitch-register writes — so an armed mode-0 LFO makes the SPU pitch register oscillate around the Note-dispatched baseline instead of holding it. The 2026-05-18 ICE V21 doc (E024, `ice_no_music`) isolated this path as the root of voice 21's post-`4cdb32b0` residual: Godot's seeder had re-armed sub-slot 0 from savestate residue (`active_dir=3`, square-wave `callback_idx=1`) on an audible primary where FFT's `play_sound` init (PC `0x80013D5C`) had cleared the active bit, driving a ±843 pitch oscillation (15464/13777 vs PCSX's constant 14596) and a 5.5× `probe_pitch_register` over-fire; smd-player's `is_silent_driver` seeder gate (iter-35) restored parity. The 2026-05-17 V21 pitch-register over-fire doc (E024, `ice_no_music`, post-`ff3104e5`) pins the per-IRQ clear discipline of the prestage→drain→walker chain (the drain zeroes `chan_word_1` after each pass and the walker zeroes `slot+0x4` after fan-out, so each prestage yields at most one SPU register write per IRQ), the three-way mode-byte dispatch table (0=pitch/chan+0x88/0x200, 1=vol-L/chan+0x8A/0x100, 2=vol-R/chan+0x8C/0x100, s0 = chan_base+0x2 corrected), and V21's sub_0 savestate config (countdown 1, callback_idx 1, ±0x01000000 accumulator → ±0x100 pitch swing per IRQ), and records the pre-fix Godot direct-walker-flag bypass (104 vs 19 fires with `probe_pitch_staging` still paired) that routing the mode-0 fire through `CHAN1_PITCH_PRESTAGE` closed.

## Points

- **FFT's LFO mode-0 (pitch) path — the sub-slot 0 swap+accumulate block at `0x800175A4`..`0x800175E0` — decrements the sub-slot countdown, swaps the accumulator's sign on wrap, and on every fire arms a pitch prestage by writing `chan_word_1 |= 0x200` (`CHAN1_PITCH_PRESTAGE`); the per-tick drain (FUN_80017118) then commits a SPU pitch-register write (walker `WALKER_FLAG_PITCH`) for each prestage it sees — so on PCSX an SPU pitch register is written only when the bytecode dispatches a Note or the active mode-0 LFO fires, which is why a cleared sub-slot 0 leaves the register holding its Note-dispatched value.** — `[S·D·R] 3/3`
  - S: `0x800175A4`..`0x800175E0` (mode-0 swap+accumulate; `chan_word_1 |= 0x200` per `ef93e550`), drain commit via FUN_80017118 — `project-assets/fft-rom/scus_disassembly.txt` (doc §8.1, §3.1)
  - D: `probe_pitch_register` on `ice_no_music` V21 — PCSX 19 sparse Note-driven writes (value 14596 held across many cadences, next write 2298 at the cad-104 Note) vs pre-fix Godot 104 writes oscillating 15464/13777 every other cadence (5.5× over-fire, 2026-05-18)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/advance_lfo.gd:230-231` (mode-0 fire sets `CHAN1_PITCH_PRESTAGE`) + `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd:495-496` (drain commits `WALKER_FLAG_PITCH`) — validated by the `probe_pitch_register` pair in `smd-player/workspace/orchestrator/probe_validation_manifest.py`
  - src: `research/effect_sound/working_documents/ICE_V21_PITCH_LFO_SUBSLOT_0_SEED_DEFICIT.md`
- **The square-wave LFO callback (`LAB_80017648`, sub-slot jumptable index 1, the one ice V21's savestate sub_0 carries via `callback_idx=1`) decrements the sub-slot's 16-bit countdown at sub+0x10 and swaps the step on wrap — with mode 0 that produces the ±step alternation (accumulator −16777216 / +16777216 at the swap cadence on ice V21) the pre-fix Godot seeder was erroneously running on an audible primary.** — `[S·R] 2/3`
  - ⚠ SUPERSEDED (2026-08-21) by: the 16-entry LFO swap-callback jumptable at `PTR_LAB_80028F54` (ram:0x80028F54–0x80028F80) maps idx 0 → LAB_80017648 and idx 1 → LAB_80017690 (mode-1 swap) — so LAB_80017648 is jumptable idx 0, not idx 1
  - S: `LAB_80017648` (16-bit countdown at `0x10(a0)`, step swap on wrap; `callback_idx=1` per ice V21's savestate) — `project-assets/fft-rom/scus_disassembly.txt` (doc §8.1)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/advance_lfo.gd` wf_idx==0 branch (port of `LAB_80017648`: accumulator toggles 0 ↔ `lfo_sub_step_source[0]`) — validated by the `probe_lfo_subslot0_state` accumulator/`active_dir` pairs in `smd-player/workspace/orchestrator/probe_validation_manifest.py`
  - src: `research/effect_sound/working_documents/ICE_V21_PITCH_LFO_SUBSLOT_0_SEED_DEFICIT.md`
- **The mode-0 commit adds the callback return's high 16 bits to `chan+0x88` at PC `0x800175A4` (`chan+0x88 += (callback_return >> 16)`), and because `per_channel_tick` pre-clears `chan+0x88` to 0 before every `lfo_handler_tick` call, each IRQ contributes exactly one bounded high-16 step to the pitch — for the wf_idx=4 sawtooth that is 0/+400, and PCSX's sign-bounded sawtooth is why the cad-328–333 `raw_pitch` outlier (Godot 7886–13424 vs PCSX ~150) is purely the pre-fix Godot sign-flip hitting the s16/u16 wraparound.** — `[S·D·R] 3/3`
  - S: `0x800175A4` (mode-0 commit add) — `project-assets/fft-rom/scus_disassembly.txt` (doc §3)
  - D: `probe_envelope_tail.raw_pitch` voice 19, `haste_no_music` (2026-05-24) — 154/274 divergent rows pre-fix; cad-328–333 outlier: Godot 7886–13424 vs PCSX ~150 (~13k-unit delta from sign-flip wraparound)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/advance_lfo.gd` (per-IRQ `chan_88`/`chan_8a` pre-clear + mode-0 high-16 commit into `pitch_bend` via the depth-256 no-scale branch, `CHAN1_PITCH_PRESTAGE` arm) — validated by the `probe_envelope_tail` / `probe_pitch_register` voice-19 pairs in `smd-player/workspace/orchestrator/probe_validation_manifest.py`
  - src: `research/effect_sound/working_documents/V19_WF_IDX_4_SAWTOOTH_FIX.md`
- **The prestage→drain→walker chain is cleared on every pass, so each prestage yields at most one SPU register write per IRQ: the per-IRQ drain (FUN_80017118, called by spu_updater_tick at scus_decompilation.c:2158) runs per channel only while `chan_word_0 != 0` and the chan+0x2B voice-count byte is < 0x19, converts PITCH_PRESTAGE (`chan_word_1` bit 0x200) into a 0..0x3FFF pitch-staging value plus walker bit 0x4, and then zeroes `chan_word_1` at the end of the pass (`*puVar8 = 0`, scus_decompilation.c:3709 — the only outer skip is the `chan_word_0` gate, so any prestage bit set while the gate was closed persists until it reopens); the `async_commit_walker` (FUN_80014590) likewise reads the slot+0x4 flag word, fires the `spu_write_voice_*` helper for each set bit (bit 0x4 writes the SPU pitch register from the staged value at slot+0x88), then zeroes the flag word (scus_decompilation.c:1959) — a walker bit that is re-armed on every tick without this clear discipline over-fires the register on every IRQ.** — `[S·D·R] 3/3`
  - S: `0x80017118` drain (scus_decompilation.c:3632-3717; outer gate at :3657, post-drain clear at :3709) + `0x80014590` `async_commit_walker` (scus_decompilation.c:1888-1969; flag-word zero at :1959) — `project-assets/fft-rom/scus_decompilation.c` + `scus_disassembly.txt` (doc §3.1, §8.1)
  - D: `probe_pitch_register` on `ice_no_music` V21 — PCSX 19 vs pre-fix Godot 104 fires while `probe_pitch_staging` still paired 80≈81 (the pitch formula ran the same number of times on both sides; the excess register writes came from the direct walker-bit set, not the formula) (2026-05-17)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/play_sound.gd::_drain_prestage_all_slots` (the doc's `smd-player/scripts/effect_sound/play_sound.gd:1434-1482` drain, since relocated: CHAN1_PITCH_PRESTAGE arm at :1667, `WALKER_FLAG_PITCH` commit at :1715, `fun80017118_clear` pre-clear-value emit followed by `channel_word_1 = 0` — the post-drain clear port) + `runtime/shared/spu_irq_walker.gd::_fan_pitch` (walker fan-out) — validated by the `probe_pitch_register` / `probe_fun80017118_clear` pairs in `smd-player/workspace/orchestrator/probe_validation_manifest.py`
  - src: `research/effect_sound/working_documents/V21_PITCH_REGISTER_OVER_FIRE_DEFICIT.md`
- **`lfo_handler_tick`'s mode byte (chan+0xFC, read at PC `0x8001755C`) dispatches to three prestage writers: mode 0 (`LAB_8001759C`) adds the LFO output to the pitch-bend accumulator at chan+0x88 and sets `chan_word_1 |= 0x200`; mode 1 (`LAB_800175B4`) adds to chan+0x8A and sets 0x100; mode 2 (`LAB_800175CC`) adds to chan+0x8C and sets 0x100 — the drain's 0x100 bit runs the vol formula (walker bit 0x1) and its 0x200 bit runs the pitch formula (walker bit 0x4), all offsets corrected for the s0 = chan_base + 0x2 base — and smd-player's `channel_state.gd` `lfo_sub_mode` comment ("0=vol, 1=pitch, 2=vol_alt") inverts the ROM mapping, which is 0=pitch, 1=vol-L, 2=vol-R.** — `[S·D·R] 3/3`
  - S: `LAB_8001759C` / `LAB_800175B4` / `LAB_800175CC` (scus_disassembly.txt:13554-13593) + drain bit semantics in FUN_80017118 (scus_decompilation.c:3659, 3696) — `project-assets/fft-rom/` (doc §3.1, §4.3)
  - D: `ice_no_music` `last_run/entity_state.json` V21 sub_0 (entity 2, channel 5): mode=0, callback_idx=1, active_dir=3 — the mode-0=pitch row confirmed on a live capture (2026-05-17)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/advance_lfo.gd` (the mode-0/1/2 fire paths) + `runtime/shared/channel_state.gd` `lfo_sub_mode` (the inverted doc comment, flagged for cleanup in doc §8.7) — validated by the `probe_lfo_subslot0_state` / `probe_vol_inputs` pairs in `smd-player/workspace/orchestrator/probe_validation_manifest.py`
  - src: `research/effect_sound/working_documents/V21_PITCH_REGISTER_OVER_FIRE_DEFICIT.md`
- **The ice V21 sub-slot 0 savestate carries a fully-armed pitch LFO whose state values produce a per-IRQ swing: mode 0, callback_idx 1 (the `LAB_80017690` swap callback), outer countdown 1 — the PC `0x80017500`-`0x80017518` lhu/sh path skips the store once the countdown reaches 0, so the callback fires on every IRQ from IRQ 2 onward — and inner_reload 1 with accumulator −0x01000000, so the swap callback (which on wrap toggles the direction bit via `xori 0x8` at PC `0x800176D0` and negates the accumulator, committing it with `sw a1, 0x4(a0)` at 0x800176CC) inverts the accumulator on every call: `lfo_current_output` alternates ±0x01000000, i.e. `output_hi` swings ±0x100 (≈ one semitone at mid range) every IRQ; active_dir=3 = active bit 0x1 (consumed by `lfo_handler_tick` at PC `0x800174EC`) + undocumented bit 0x2, and Godot's seeder remaps a zero countdown to 1 ("fire immediately", matching the PC `0x80017500` `beq v0, zero`).** — `[S·D·R] 3/3`
  - S: `LAB_80017690` (scus_disassembly.txt:13642-13665; `xori v0, v0, 0x8` at PC 0x800176D0; swap commit at 0x800176CC) + countdown store-skip PC `0x80017500`-`0x80017518` + active-bit gate PC `0x800174EC` — `project-assets/fft-rom/scus_disassembly.txt` (doc §5, §8.1)
  - D: `ice_no_music` `last_run/entity_state.json` sub_0 (accumulator −16777216, step_source +16777216, countdown 1, inner_reload 1, depth 256, mode 0, callback_idx 1, active_dir 3) (2026-05-17)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/play_sound.gd` sub-slot-0 seeder (:623-630, the countdown 0→1 remap with the in-code comment citing PC 0x80017500; the doc's `play_sound.gd:482-501`) + `runtime/shared/per_tick/advance_lfo.gd` default branch (wf_idx==1 / `LAB_80017690` swap-with-negation port) — validated by the `probe_lfo_subslot0_state` accumulator/`active_dir` pairs in `smd-player/workspace/orchestrator/probe_validation_manifest.py`
  - src: `research/effect_sound/working_documents/V21_PITCH_REGISTER_OVER_FIRE_DEFICIT.md`
- **At the doc date (post-`ff3104e5`, 2026-05-17) smd-player's `_advance_lfo` set `slot.walker_flag_word |= WALKER_FLAG_PITCH` directly on every cadence the LFO sub-slot was active, bypassing the `chan_word_1` prestage chain — the prestage drain was entered only for porta updates, and the LFO-aware elif read the walker flag and then set the prestage, the reverse order of FFT — with the diagnostic signature that `probe_pitch_staging` still paired (PCSX 80 vs Godot 81) while `probe_pitch_register` over-fired (84 vs 167 total; V21 19 vs 104) and `probe_walker_flag_word_entry` ran +16 (448 vs 464), localizing the defect to the walker-flag path rather than the pitch formula; the mode-0 fire now routes through `CHAN1_PITCH_PRESTAGE` before the walker bit, matching the doc's Candidate A.** — `[D·R] 2/3`
  - D: `ice_no_music` post-`ff3104e5` probe counts — `probe_pitch_register` 84 vs 167 (V21 19 vs 104), `probe_pitch_staging` 80 vs 81, `probe_walker_flag_word_entry` 448 vs 464, `probe_vol_formula_stages` 1656 vs 1748 (2026-05-17)
  - R: pre-fix `smd-player/scripts/effect_sound/dispatcher.gd:458-468` (direct `WALKER_FLAG_PITCH` set in `_advance_lfo`; LFO-aware PRESTAGE elif at :648-669, porta-only prestage entry at :629-647 — old path, since relocated/refactored) → current `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd:495-496` (`CHAN1_PITCH_PRESTAGE` then `WALKER_FLAG_PITCH`) — validated by the `ice_no_music` orchestrator probe pairs (`smd-player/workspace/orchestrator/probe_validation_manifest.py`)
  - src: `research/effect_sound/working_documents/V21_PITCH_REGISTER_OVER_FIRE_DEFICIT.md`

## Notes

(empty — user territory)

## Related

- [[LFO Callback Jumptable]]
- [[LFO Subslot Residue]]
- [[Noise LFO PRNG]]
- [[LFO Sub-Slot Period Reset]]
- [[SPU Voice Engine]]
- [[PSX Pitch Conversion]]
- [[Effect Sound Audio Divergence]]
