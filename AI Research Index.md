# AI Research Index

Root index for the AI Research vault. Link new notes here.

## Topics

- [[Ability Execution Index]] — charging/execution animations, the tables that select them, and the battle state machine
- [[Cinematic Rendering Index]] — EVTCHR sprite rendering: CLUT resolution, atlas rows, the per-unit anim-script VM
- [[Combat Damage Index]] — element defense, element/status byte encoding, and terrain- or status-driven damage modifiers
- [[Display Space Compositing Index]] — PSX ABR add/sub in 8-bit sRGB, and the engine fold that forces display-space compositing
- [[Effect System Index]] — E###.BIN palette tracks, colour interpolation, script opcodes, TRAP charge-effect particles
- [[Event VM Index]] — the cutscene bytecode interpreter: opcode parameter blocks, dialog/text, the event variable system
- [[Formation Screen Index]] — the roster/ability/equipment UI: compositing model, per-cell element placement, panels and pickers
- [[Map Systems Index]] — mesh-resource headers and their animation/state mechanisms, GNS state selection, terrain rendering
- [[PCSX Capture Rig Index]] — savestate loading, post-load frame timing, live-memory breakpoints, SPU event capture + replay
- [[PSX Rendering Index]] — the Ordering Table depth model, GTE transforms, and the depth pipeline terrain/units/effects share
- [[Scenario Index]] — ATTACK.OUT joining map + song + deployment into one battle, and the event model that bundles them
- [[SFX Index]] — SFX banks and their event opcodes, the FEDS/SMD format, effect sound scheduling, the shared SPU voice engine
- [[Unit Deployment Index]] — the ENTD deployment table, its BATTLE.BIN loader chain, and sprite-set → SPR resolution
- [[Unit Index]] — the unit-build pipeline: stat growth, effective-stat derivation, ability/gambit wiring
- [[Unit Sprite Index]] — how sprite-set data and runtime facing become on-screen sprites, in both PSX sprite renderers
- [[World Map Index]] — `WLDCORE.BIN` layout and render pipeline, the entry load, and dispatch between scenario runs

## Cross-cutting

- [[Needs Review]] — the register of open `CONTESTED` contradictions

## Setup

- [[Local Qwen Coder]]
