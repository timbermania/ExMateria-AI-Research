# Unit Anim Opcode

Event instruction `{11}` Unit Anim plays an animation on one unit or on a team (selected by the Multi Targeting byte): the half-word Animation ID either names a unit SEQ animation (x0000–x003B idle/locomotion/state, x003C–x007D weapon attacks that require a clear EVTCHR block) or an EVTCHR atlas animation (Block 1 x01F4–x0233, Block 2 x0258–x0297, slot-specific). The four ID buckets map to two live runtime paths — SEQ animations are driven by the combat per-unit ticker (`FUN_8017a290` → `FUN_801810a0`) and EVTCHR animations by the cinematic dispatcher (`FUN_80084818` / `update_and_animate_unit_wep_eff`, `FUN_80085c0c`) — and the Orbonne chapel cinematic runs entirely on the EVTCHR path (ticker 0 fires, dispatcher ~6800 fires over 30 s). The spec is the ffhacktics wiki `Event_Instruction_11` article, pasted 2026-06-27 during the chapel sprite-pipeline investigation; the opcode is implemented in the Godot reimplementation (`_op_unit_anim` in `ScenarioVM`), per the master-catalog snapshot (2026-06-30) and verified in current `godot-learning/src/scenarios/ScenarioVM.gd`. The unit's current animation ID is readable in RAM as a uint16 at `unit+0x1DC`, and PSX and Godot (`Unit.current_anim_id`) agree exactly at the same parked beat PC (verified at scn6 PC210: Delita 520, Ovelia 510). The PSX cinematic path itself splits into a mid band `[0x1F4, 0x258)` (table `0x800A77D8`, `slot = anim − 0x1F4`) and a high band `[0x258, …)` (table `0x800AED3C`, `slot = anim − 0x258`): the Orbonne chapel drives the high band, scenario 6's carry drives the mid band, and Godot now routes `{11}` body-anim IDs by this PSX band split (`_cinematic_band_base`) with the renderer's EVTCHR boundary at `0x1f4`. The pose is not painted at `{11}` dispatch: the handler latches `unit+0x0C = anim_id+1` and the per-frame consumer paints `+0x1DC` on the next elapsed frame (inside the following Wait), so Godot mirrors the pose as a `pending_anim` latch consumed only while the main context is blocked. In the Orbonne chapel the priest (chunk unit 19, sprite_set 0x13, TYPE2-SHP/TYPE3-SEQ) slides forward on a plain `Sprite Move`, and his walk cycle rides on the low-range `Unit Anim` (event `0x03` walk / `0x02` idle); the routing is now fully decoded and live-verified — the uniform player-side formula `(slot−1)*2 + facing_base` indexes the unit's own per-type SEQ table (class byte at `DAT_80094748`, table pointer at `unit+0x1F8`), so walk `0x03` lands on the 9-frame slot 6 and idle `0x02` on the 1-frame slot 4 — and Godot mirrors it by writing `event_id + 1` straight into `current_anim_id` on the low-range path (fixed 2026-06-28, user-confirmed: the priest's feet now cycle).

## Points

- **Event instruction `{11}` Unit Anim's body is, after the opcode byte: Unknown (1B, "mostly seems to be x00 and sometimes x01"), Affected Units (1B), Multi Targeting (1B), Animation ID (half-word) — where the Animation ID loads an animation sequence from the corresponding SEQ file for a unit, or an EVTCHR animation.** — `[ ] 0/3`
  - src: `research/wiki_articles/event_instruction_11_unit_anim.md`
  - ⚠ SUPERSEDED (2026-08-19) by: The {11} body is read by the handler as {u16 chunk_unit_id, u16 anim_id, u8 flag} — the Animation ID halfword is at payload bytes 2–3 (handler reads it via `event_bytecode_reader_c` at `payload+2`), not 3–4
- **The Multi Targeting byte selects how Affected Units is interpreted — x00: one target, Affected Units is the ID of the unit specified in the ENTD; x01: team codes (x00 Player's Team (blue), x01 Player's Team (blue) unless incapacitated (dead, critical, etc.), x02 All Enemy Teams (red/green/lightblue), x03 All Enemy Teams unless incapacitated, x04 All Teams); x02: target all teams.** — `[ ] 0/3`
  - src: `research/wiki_articles/event_instruction_11_unit_anim.md`
- **Animation IDs x0000–x003B are unit SEQ animations (x0000/x0032/x003B Vanish, x0001/x0002 Standing, x0003 Walk, x000D Run, x0012–x0014 Fly-to, x0015 Spinning, x0016 Critical/Bow, x0017 Defend, x0018 Dodge, x0019 In Pain (loop), x001A Dead, x001C Level Up (loop), x001D Job Level Up (loop), x0021/x0022 Charge, x0025 Confusion, x0034 Character Death, x0035/x0036 Bow, x0064 Flip Switch On Ground), and a "Nothing" ID (x003A) is a buffer that pauses on whatever the unit's last animation frame was.** — `[ ] 0/3`
  - src: `research/wiki_articles/event_instruction_11_unit_anim.md`
- **Animation IDs x003C–x007D are SEQ weapon/attack animations (unarmed attacks x003D–x003F, weapon attacks x0040–x0042, throws x0043–x004C, spear x004D–x004F, gunshot x0050–x0052, bow x0053, book strike x0056, weapon guards x0058–x005A, shield recoil x005B–x0063, Lancer Jump x0075/x0076, female weapon x0077–x0079, Goblin Punch x007D) and every one of them requires an EVTCHR block to be clear — with two EVTCHR blocks Loaded and Saved you cannot use a weapon attack animation; the wiki directs `LoadEVTCHRClear`/`SaveEVTCHRClear`.** — `[ ] 0/3`
  - src: `research/wiki_articles/event_instruction_11_unit_anim.md`
- **EVTCHR animations occupy IDs x01F4–x0233 (Block 1, 64 slots, red) and x0258–x0297 (Block 2, 64 slots, blue) and are slot-specific — each animation index maps to a specific grid cell of the EVTCHR atlas, with `EVTCHR_LOCATIONS.png` as the canonical visual slot map for both blocks.** — `[ ] 0/3`
  - src: `research/wiki_articles/event_instruction_11_unit_anim.md`
- **The four Unit Anim ID buckets map to two live runtime paths: SEQ animations (x0000–x003B, x003C–x007D) go through the combat per-unit ticker (`FUN_8017a290` → `FUN_801810a0`, the sprite pipeline's "Layer 4"), while EVTCHR animations (x01F4–x0233, x0258–x0297) go through the cinematic dispatcher (`FUN_80084818` / `update_and_animate_unit_wep_eff` = `FUN_80085c0c`).** — `[S] 1/3`
  - S: `FUN_8017a290` → `FUN_801810a0` (per `SPRITE_PIPELINE_INVESTIGATION.md` Layer 4); `FUN_80084818` / `update_and_animate_unit_wep_eff` = `FUN_80085c0c` (per `reference_cinematic_seq_table_2026_06_25`)
  - src: `research/wiki_articles/event_instruction_11_unit_anim.md`
- **During the Orbonne chapel cinematic the combat ticker `FUN_8017a290` fires 0 times while the cinematic dispatcher `FUN_80085c0c` fires ~6800× over 30 s — the chapel renderer is the EVTCHR path.** — `[S·D] 2/3`
  - S: `FUN_8017a290`, `FUN_80085c0c`
  - D: probe `reference_cinematic_seq_table_2026_06_25` (2026-06-25)
  - D: probe `probe_layer4_render.lua` 30 s breakpoint hit table, `orbonne_prayer_mid_dialog.sstate` (2026-06-27): FUN_8017a290=0, FUN_80085c0c=6802, FUN_80084818=60
  - src: `research/wiki_articles/event_instruction_11_unit_anim.md`
- **The `{11}` Unit Anim handler is `0x80149398` (`evt0x11_unit_anim_handler`), which reads the chunk and dispatches on the Animation ID.** — `[S] 1/3`
  - S: `0x80149398` (`evt0x11_unit_anim_handler`) (`battle_disassembly.txt`, master catalog `EventCommands.xml` row {11})
  - src: `research/wiki_articles/event_instructions.md`
- **A unit's current animation ID is stored as a uint16 at `unit+0x1DC` (scn6: Delita unit record base `0x800B8848`, Ovelia `0x800BA608`), and read at a parked beat the PSX value equals Godot's `Unit.current_anim_id` at the same PC — verified at scn6 PC210: Delita 520, Ovelia 510, exact match.** — `[S·D·R] 3/3`
  - S: anim ID at `uint16 @ unit+0x1DC`, unit record bases `0x800B8848`/`0x800BA608` (doc)
  - D: anim-ID read at the scn6 PC210 park — exact PSX/Godot match, Delita 520 / Ovelia 510 (2026-07-07)
  - R: `godot-learning/src/units/Unit.gd` (`current_anim_id`) (no named test)
  - src: `research/working_documents/CAPTURE_SCENARIO_BEAT_HOWTO.md`
- **Godot's `{11}` Unit Anim now routes body-anim IDs by the PSX band instead of the chapel-specific `>= 0x258` gate: `_cinematic_band_base` classifies low `[1, 0x1F4)` as −1 (plain SEQ path), mid `[0x1F4, 0x258)` as `0x1F4`, and high `[0x258, …)` as `0x258`; the local index is `anim − band_base` per band, the EVTCHR block comes from the most recent `Load EVTCHR` (`_active_evtchr_block`: scn6 = 0, chapel stays at the default 2), and the segment is that live Slot; `UnitDisplay`'s EVTCHR paint/clock boundary was lowered `0x1f5` → `0x1f4` (the true band start — a raw-written cinematic 0x1F4 used to fall into the SEQ paint branch; the only aliasing value, low-range event anim 499 `+1`-bumped to 0x1F4, is unused). Headful-verified: Delita hoists Ovelia over his shoulder on the tower ledge, and the mid band is fixed for all its scenarios (6, 29, 43, 54, 58, …).** — `[D·R] 2/3`
  - D: `probe_scenario6_carry_anim.gd` after the fix (2026-07-05): both leads show `walker=true` the tick each fires (before the fix `walker=false` through the whole cutscene, every carry anim in the `EVTCHR-noop` branch); headful capture at MAXF=1910 renders the carry pose
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` (`_cinematic_band_base`, `_active_evtchr_block`, `_resolve_cinematic_segment`) + `godot-learning/src/animation/UnitDisplay.gd` (`_paint_body_variant` / `_arm_anim_id_clock` boundary `0x1f4`) + `godot-learning/tests/ScenarioCinematicBandRoutingTest.gd` (15)
  - src: `research/working_documents/SCENARIO6_CARRY_POSE_EVTCHR_RENDER.md`
- **The cinematic carry-pose H-flip is set deterministically per frame from facing + camera — `CinematicWalkState._render` computes `AnimationStateController.get_camera_variant(facing, camera_quad)["revert"]` and pushes `slm.set_global_reversion` — instead of the stale `global_reversion` left over from each unit's last pre-cinematic paint (which mirrored Ovelia off the wrong side of the chocobo even though she and Delita both face South `0x400` under the same camera at PC=316); both South leads now resolve `revert=true`, matching PSX.** — `[D·R] 2/3`
  - D: `probe_scenario6_reversion.gd` + `probe_scenario6_ovelia_flip.gd` headful capture at PC=316 (2026-07-06): both units `revert=true`; the tableau matches `scenario6_carry_images/00_baseline.png`
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` (`CinematicWalkState._render` / `_apply_cinematic_reversion`) + `godot-learning/src/animation/AnimationStateController.gd` (`get_camera_variant`) — validated by ScenarioApplyTest 158/0, ScenarioCinematicBandRoutingTest 15/0, ScenarioCinematicWalkerTest 23/0
  - src: `research/working_documents/SCENARIO6_CARRY_POSE_EVTCHR_RENDER.md`

- **[S] 1/3** — The `{11}` body is read by the handler as `{u16 chunk_unit_id, u16 anim_id, u8 flag}`: `evt0x11_unit_anim_handler @ 0x80149398` first resolves `chunk_unit_id` to a unit handle (`FUN_80147928 @ 0x80147928`), loops matching units (`FUN_801479AC @ 0x801479AC`, ≤0x15 units), then reads `anim_id` at `payload+2` via `event_bytecode_reader_c` — so the Animation ID halfword is at payload bytes 2–3, not 3–4 — and routes through `BATTLE_set_unit_anim_value @ 0x8012DA0C`; for carry-band `anim ≥ 0x1F4` that path falls through to `trigger_unit_animations_by_param @ 0x8008BE7C` → `set_unit_animation_with_flags @ 0x80081978` (the latch writer), and the `flag` byte (payload+4) only toggles the 0x4000 bit of the move/misc flag — it never carries the pose.
  - S: `evt0x11_unit_anim_handler` payload read (`payload+2` → anim id), `FUN_80147928 @ 0x80147928` / `FUN_801479AC @ 0x801479AC` chunk-unit resolution, `BATTLE_set_unit_anim_value @ 0x8012DA0C` (`anim < 0x1F4` / `≥ 0x1F4` split), `trigger_unit_animations_by_param @ 0x8008BE7C`, `set_unit_animation_with_flags @ 0x80081978` (battle_decompilation.c + battle_disassembly.txt)
  - S: the `flag` byte dispatches to `BATTLE_unset_misc_data_move_flag_bit_1` (flag=0) or `BATTLE_flip_misc_data_move_flag_bit_1` (flag=1), else no-op (BATTLE.BIN symbols, handler-chain trace 2026-06-24)
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` (`_op_unit_anim` → `ScenarioApply.unit_anim`, same operand order) + `godot-learning/tests/ScenarioApplyTest.gd` (169/0)
  - src: research/working_documents/SCENARIO_WAIT_SEMANTICS.md

- **[S·D·R] 3/3** — `{11}` does not paint the pose at dispatch: it latches `unit+0x0C = anim_id + 1` (literal `addiu` at `0x80081978`, `sh a0,0xc(a2)` at `0x8008197C` inside `set_unit_animation_with_flags @ 0x80081978`), and the per-frame consumer `FUN_8008719C @ 0x8008719C` → `FUN_80085C0C @ 0x80085C0C` reads the latch (`0x80085DF4`), paints the anim id into `+0x1DC` (writes @ `0x80085E40`/`0x800861B8`/`0x80086350`/`0x8008637C`), zeroes `+0x1DE/+0x1E2`, then clears the latch (`sh zero,0xc(s2)` @ `0x80085EA4`/`0x80086238`/`0x80086304`) — so the pose lands on the first elapsed frame *after* the `{11}`, i.e. inside the following Wait, never on the `{11}` opcode itself (queue-on-opcode, paint-on-frame).
  - S: writer chain `0x801448C4`→`0x801448CC` dispatch, `evt0x11_unit_anim_handler @ 0x80149398`, `BATTLE_set_unit_anim_value @ 0x8012DA0C`, `trigger_unit_animations_by_param @ 0x8008BE7C`, `set_unit_animation_with_flags @ 0x80081978` (`sh` @ `0x8008197C`); consumer `FUN_8008719C` → `FUN_80085C0C` (latch read `0x80085DF4`, paint `0x80085E40` et al., clear `0x80085EA4`/`0x80086238`/`0x80086304`) (battle_decompilation.c + battle_disassembly.txt)
  - D: write-BP on the latch field (Delita `+0x0C` = `0x800B8854`, Ovelia `+0x0C` = `0x800BA614`) + single-pass `scn_step` sweep pc216→225 (2026-07-08, pcsx port 8080): every hit is writer PC `0x8008197C` or clearer PC `0x80086238`; pre-clear values are anim+1 (520 = 519+1, 526 = 525+1, 513 = 512+1); the clearer fires on the first frame of both the `{F1}`@217 and the `{6F}`@224 waits
  - D: `probe_chapel_opcodes.lua` chapel BP against the `0x80149398` Unit-Anim dispatcher (2026-06-28): samples roster `+0x0C current_anim_slot` + `+0x70 facing` per slot for the priest (uid 0x13)
  - R: `godot-learning/src/scenarios/ScenarioActor.gd` (`pending_anim` latch) + `godot-learning/src/scenarios/ScenarioVM.gd` (`_apply_unit_animation` latches only, `_paint_unit_animation`/`_consume_pending_body_anims` paint on tick) + `godot-learning/tests/ScenarioUnitAnimLatchTest.gd` (10/0)
  - src: research/working_documents/SCENARIO_WAIT_SEMANTICS.md

- **[S·D·R] 3/3** — A pending-pose latch persists across non-blocking opcodes and across parks — a frame elapses only while the main thread yields inside a blocking wait — so the paint lands on the first frame of the next *blocking* wait: in the scn6 carry the PSX paints at park 213 (`{11}`@210, surviving the `{3B}`@211 arm), 218 (`{11}`@216), 225 and 225 (`{11}`@220/221, surviving up to three non-blocking opcodes until the `{6F}`@224); Godot's consume is gated on `_main_ctx_blocked()` (it never fires while parked or while stepping over a non-blocking arm) and matches PSX park-for-park on all four paints.
  - S: static model §6b — the consumer runs on yielded frames and frames elapse only when the main thread blocks: `FUN_8014CA80 @ 0x8014CA80` yield, dispatcher `FUN_80143BD8 @ 0x80143BD8` runs non-blocking opcodes without yielding (battle_disassembly.txt)
  - D: single-pass `scn_step` sweep pc200→225, base `scenario6_letgo_full_base` (2026-07-08): paint parks 213 / 218 / 225 / 225; pre-exec RAM polling at the 217/218 parked states shows the latch set to 520, then cleared with `+0x1DC` repainted
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` `_advance_frame` gate (`if _main_ctx_blocked(): _consume_pending_body_anims()`) + `godot-learning/tests/ScenarioLatchStepBoundaryTest.gd` (14/0, red-first verified) + `tools/probe_park_freeze.gd` pc211 GREEN
  - src: research/working_documents/SCENARIO_WAIT_SEMANTICS.md

- **[S·D·R] 3/3** — The carry/cinematic pose (anim ≥ 0x1F4) is not a separate animation engine or texture-streamed frames — it is an ordinary struct-driven SEQ animation keyed on `+0x1DC`: the per-frame consumer paints the anim id identically for all bands, the drawn quad `*(unit+0x204)` changes only when a `{11}` supplies a new anim id, and the delta is a single UV byte selecting a different frame of the already-loaded static EVTCHR sheet (the sheet at VRAM x=832 is unchanged across pc200→225); a redundant `{11}` with the same id re-resolves to the identical quad.
  - S: `BATTLE_set_unit_anim_value @ 0x8012DA0C` routes `anim ≥ 0x1F4` into the same `trigger_unit_animations_by_param @ 0x8008BE7C` / consumer chain; the sheet load chain (`evtchr_slot_allocator @ 0x80083CD4`, decode `FUN_8007AA34 @ 0x8007AA34`, LoadImage `func_0x800248fc`) exists but is not on the per-frame anim path (battle_decompilation.c)
  - D: per-unit primitive diff `*(unit+0x204)` across a single-pass sweep pc200→218 (2026-07-08): Δ only on anim-id steps (Ovelia 509→510, 510→511; Delita 520→519), a single UV byte at primitive offset +18 (96→128); the pc203→204 beat is byte-identical on both carry primitives — the visible change there is the onlookers' slot 6 + the double-buffer tail
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` (cinematic walker resolves frames struct-driven from the pre-baked sheet, no streaming) + `godot-learning/tests/ScenarioCinematicWalkerTest.gd` (23/0)
  - src: research/working_documents/SCENARIO_WAIT_SEMANTICS.md

- **[D·R] 2/3** — Godot's carry-pose chain is byte-exact with PSX for every frame verified: `anim_id → local_idx (anim − 0x1F4) → LoadFrameWait frame byte` equals PSX `+0x1E0`, and the `evtchr_frames.json` rect equals the PSX primitive UV — Ovelia 509/510/511 → frames 227/228/229 → U 96/128/160 (V 80), Delita 520/519 → frames 243/242 → U 160/128 (V 160).
  - D: PSX frame timeline from the per-unit `*(unit+0x204)` UV bytes (`[+18]=U`, `[+19]=V`) across pc200→218, single-pass sweep (2026-07-08)
  - R: `godot-learning/assets/sprites/animations/cinematic_seq.json` + `evtchr_frames.json` (segment 1, `atlas_y_offset` default 0) static-diffed byte-exact; guarded by `tests/ScenarioCinematicWalkerTest.gd` (23/0), `tests/ScenarioCinematicBandRoutingTest.gd` (15/0), `tests/ScenarioLatchStepBoundaryTest.gd` (14/0), `tests/ScenarioUnitAnimLatchTest.gd` (10/0)
  - src: research/working_documents/SCENARIO_WAIT_SEMANTICS.md

- **The Orbonne chapel's priest is chunk unit 19 (uid 0x13, sprite_set 0x13), classified in `sprite_types.json` as SHP `TYPE2` + SEQ `TYPE3` (as are the chapel rotators, sprite_set 0x13): his body renders from `TYPE3.SEQ`, so he slides fine even while the walk cycle is missing — the file routing was working.** — `[R] 1/3`
  - R: `godot-learning/assets/abilities/sprite_types.json` entries "19"/"13" (`shp: TYPE2`, `seq: TYPE3`, `seq_file: TYPE3.SEQ`); no test named pins the shp/seq classification
  - src: `research/working_documents/scenario_1_captures/HANDOFF_priest_walk_animation.md`
- **In the parsed `TYPE3.SEQ`, slot 4 is a single-frame `LoadFrameWait` static pose — it renders one pose and never cycles — while slot 8 is a 9-frame sequence; slot 4 is what the pre-fix Godot path armed for the priest's walk event, which is why his legs never cycled during the slide.** — `[D·R] 2/3`
  - D: `type3_seq.json` dump (2026-06-28): slot 4 = 1 frame (`LoadFrameWait`), slot 8 = 9 frames
  - R: `godot-learning/assets/sprites/animations/type3_seq.json` + `godot-learning/tests/ScenarioEventAnimTypeAwareTest.gd` (cross-checks slot 4 = the 1-frame pose; registered in `tests/run_all_tests.sh`)
  - src: `research/working_documents/scenario_1_captures/HANDOFF_priest_walk_animation.md`
- **The chapel priest's forward step toward Ovelia is a plain `Sprite Move` (+X=28, Time=40), not Walk To: inst 73 `Unit Anim` (walk) → inst 75 `Sprite Move` → inst 76 `Wait Sprite Move` → inst 77 `Unit Anim` (idle), so the walk cycle relies entirely on the low-range `Unit Anim` keeping the body SEQ playing during the slide, and the Walk To `WALKING`-activity hook does not apply here.** — `[D·R] 2/3`
  - D: chapel trace `last_run/godot_all_units_trace.jsonl` (2026-06-28, `SCENARIO_PLAY_THROUGH` burn-through): pc 73 arms the walk on uid 0x13 (`in_cinematic_mode=False`), pc 75–76 the slide, pc 77 back to idle
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` (`_op_unit_anim` low-range SEQ path + `_op_sprite_move`) + `godot-learning/tests/ScenarioSpriteMoveTest.gd`
  - src: `research/working_documents/scenario_1_captures/HANDOFF_priest_walk_animation.md`
- **Combat animation in the reimplementation is type-aware by construction: every combat anim goes through `AnimationResolutionMap.resolve_*(sprite_type, …)` (`Unit.attack()` → `resolve_attack(sprite_type, …)`, activity path → `resolve_for_activity(activity, sprite_type, …)`), which returns the correct per-type `body_slot` from the per-type SEQ tables (`state_animations.json`) — that is why TYPE2/TYPE3 units pick the right frames in battle, and the gap the cinematic low-range `Unit Anim` path had, since `sprite_type` was never consulted there.** — `[R] 1/3`
  - R: `godot-learning/src/animation/AnimationResolutionMap.gd` (`resolve_for_activity`, `resolve_attack`) + `godot-learning/src/units/Unit.gd` (`attack()` → `resolve_attack`, activity path → `resolve_for_activity`) — no test named
  - src: `research/working_documents/scenario_1_captures/HANDOFF_priest_walk_animation.md`
- **The low-range event `Unit Anim` ids are small well-known ids: `0x02` = idle (at-ease), `0x03` = walk.** — `[D·R] 2/3`
  - D: chapel trace (2026-06-28): inst 73 sets anim 3 (walk) ahead of the priest's slide, inst 77 back to anim 2 (idle)
  - R: `godot-learning/src/animation/CinematicPoseLUT.gd` (chapel "at-ease" comment on `event_anim_id == 2`) + `godot-learning/tests/ScenarioEventAnimTypeAwareTest.gd` (pins idle `0x02`→4, walk `0x03`→6)
  - src: `research/working_documents/scenario_1_captures/HANDOFF_priest_walk_animation.md`
- **The event Unit Anim → SEQ-frame mapping is two stages, and only the second is sprite-type-aware: the per-frame player (`FUN_80085c0c` @ `0x80085C0C`) computes `seq_offset = (unit[+0x0C] − 1)*2 + facing_base[(facing >> 10) & 3]` (`facing_base = {0,1,1,0}` from `DAT_800680E4`) and indexes the unit's own per-type SEQ table pointer at `unit+0x1F8` (gated `off < seqtab[1]`) — there is no sprite-type branch in the index math, so event `0x03` (walk) → offset 6 and `0x02` (idle) → offset 4 for every type; the chapel priest's walk was live-verified at offset 6 in the class-1 table `0x800BFE38` (the 9-frame walk vs the 1-frame idle at 4).** — `[S·D·R] 3/3`
  - S: `FUN_80085c0c` @ `0x80085C0C` (core loop `iVar6 = +0x0C − 1; off = iVar6*2 + facing_base[quad]; seqtab = *unit[+0x1F8]; if (off < seqtab[1]) select/advance`), `facing_base` = `DAT_800680E4` (`battle_disassembly.txt`; export confirmed FRESH — a live ±0x800 breakpoint sweep fired only on the documented addresses, no base shift)
  - D: `probe_anim_routing_verify.py` (2026-06-28, `orbonne_priest_walk.sstate`): `+0x0C=0x04 fac=0x0c00 quad=3 → seq_offset=6` (WALK), `+0x0C=0x03 → seq_offset=4` (IDLE), both with `seqtab+1F8=0x800bfe38`; all 72 rows of `golden_unit_animations.jsonl` (every unit across the cinematic) obey the formula
  - R: `godot-learning/src/scenarios/ScenarioVM.gd` (`_paint_unit_animation` low-range path: `unit.play_body(anim_id + 1)` — mirrors the hardware `+0x0C` latch) + `godot-learning/src/animation/UnitDisplay.gd` (`_arm_anim_id_clock`: `clock_key = str((current_anim_id − 1) * 2)`) + `godot-learning/tests/ScenarioFacingAnimDecodeTest.gd` (`_test_op_unit_anim_low_range_writes_anim_plus_one`: 0x03 → `current_anim_id` 0x04 → clock slot 6) + `godot-learning/tests/ScenarioEventAnimTypeAwareTest.gd` (pins 0x02→4, 0x03→6, 0x0E→28 against the golden fixture)
  - src: `research/working_documents/scenario_1_captures/HANDOFF_type_aware_animation_routing.md`
- **The `anim < 0x1F4` side path in the Unit Anim chain never remaps the animation: `BATTLE_set_unit_anim_value` @ `0x8012DA0C` gates on `sltiu v0,s2,0x1f4` and its side path (`FUN_80180afc` @ `0x80180AFC`) only fetches the actor record at `0x801908CC + idx*0x1C0` and schedules a deferred sprite/CLUT loader callback into `DAT_80173CA8` (`g_attack_out_loader_fptr`), while `trigger_unit_animations_by_param` @ `0x8008BE7C` re-reads only the facing from `+0x70` and forwards `anim` unchanged to the latch writer — the side path is orthogonal to SEQ index selection.** — `[S·D] 2/3`
  - S: `BATTLE_set_unit_anim_value` @ `0x8012DA0C` (0x1F4/500 threshold `sltiu`), side path `FUN_80180afc` @ `0x80180AFC` (actor record `0x801908CC + idx*0x1C0`; deferred-loader slot `DAT_80173CA8`), `trigger_unit_animations_by_param` @ `0x8008BE7C` (`lh a1,0x70(v0)` facing re-read; no anim remap) (`battle_decompilation.c` + `battle_disassembly.txt`)
  - S: side path also reads unit fields `+0x47`/`+0x48`/`+0x182` (clut/sprite/status bits) ahead of the actor-record fetch (`FUN_80180afc`, BATTLE.BIN handler-chain trace 2026-06-24)
  - D: `probe_anim_routing.py` (2026-06-28, priest walk): 3 Unit Anims → 3 hits each on `0x80149398` / `0x8012DA0C` / `0x8008BE7C` / `0x80081978`; side path `0x80180AFC` 240 hits (general per-frame accessor; `anim` unchanged through all three)
  - R: none — deferred sprite/CLUT loader (`DAT_80173CA8` / `g_attack_out_loader_fptr`) not present in godot-learning (probed `godot-learning/src/`, `godot-learning/tests/` for `0x173CA8` / loader symbols)
  - src: `research/working_documents/scenario_1_captures/HANDOFF_type_aware_animation_routing.md`

## Notes

(empty — user territory)

## Related

- [[ENTD Unit Deployment Table]]
- [[Event Opcode Catalog]]
- [[Scenario Beat Capture]]
- [[EVTCHR Script VM]]
- [[Cinematic Sprite Renderer]]
- [[Scenario 6 Carry Composition]]
- [[Scenario Wait Semantics]]
- [[Sprite Move Opcode]]
- [[Unit Sprite Object Struct]]
