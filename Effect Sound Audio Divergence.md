# Effect Sound Audio Divergence

State of the Godot effect-sound synth vs the PCSX-Redux capture for `cure_no_music` as of the 2026-05-16 post-walker-flag-fix doc (`AUDIO_PARITY_NEXT_STEPS_V2`): the walker flag word entry shortfall is closed — the end-of-note ADSR2_LOW write (commit 9bf658cc) was the single biggest mover (full_mix cos_dist 0.38→0.31, voice_21 0.47→0.37; `probe_walker_flag_word_entry` pairs 320/320 within ±1 drift, 318/320 bit-exact) — and the pitch chain re-pairs with its write cadence now FFT-faithful. Residual divergences at that date: 2 init-seed walker entries (Godot seeds 0xC5 vs PCSX's 0x1FF, missing the ADSR1/SAMPLE_ADDR/VOL_LR_SWEEP arms), 2 extra PCSX walker exits via `chan_word_0 & 0x500` (`probe_slur_propagation_pre` 81/79), a 2-row `_fan_vol_lr_raw` excess at voice-21 ramp start (`probe_vol_register` 122/124, values aligned), plus still-unprobed ADSR1-register and KON/KOFF-mask paths. The 2026-05-12 accounts below are historical: the 0.45 residual was the pre-fix baseline, and the 1-cadence pitch-offset account is superseded. The 2026-05-13 CAUSE_A_PRESTAGE_TIMING doc re-attributes the "init-seed" 0x1FF entries to bind-time AC dispatch (PC `0x80017088`), and the follow-up Godot fix (deferred `walker_seed_pending` arm, synthetic first_keyon arm removed) landed since. The 2026-05-18 death (E030) V21 deficit closed a second divergence class: Godot's 1.24 s truncation of voice 21 was the missing 3rd `play_sound` — a BATTLE.BIN-driven global-bank trigger (`0x8006B97C`, sound_id 0x45) structurally outside the effect-only renderer, resolved by smd-player's catalog replay (see [[Battle Action SFX]]). The 2026-05-24 ENV_VOL_ARCHITECTURAL_GATING doc localized the cross-session env_vol divergence (the seven `probe_envelope_tail` env-tail counts) to the SPU mixer's per-sample env_vol evolution — ADSR1/ADSR2 register writes are bit-identical across all sessions, ruling out chan-side dispatch — and refuted its own stop-flag root cause (Godot already implements the ADPCM stop-sign voice-off; see [[SPU Voice Engine]]); the GATING doc was then retracted as the wrong hypothesis (commit 0f7d2bee) and the 2026-05-24 follow-up ENV_VOL_WITHIN_IRQ_KEY_ON_TIMING doc promotes within-IRQ KEY-ON sample offset to the leading candidate — PCSX commits KEY-ON at a wall-clock-dependent SPU sample inside the IRQ (probe load stretches `samples_per_cadence` to 190–320 vs canonical 183) while Godot fires it at a fixed in-sub-tick offset, so the post-KEY-ON mix-call count per cadence differs (an estimated ~230-mix-call gap plausibly matches voice-20's Δ≈7328 env); still open pending the doc's §4 KEY-ON-offset diagnostics. The 2026-05-19 flare (E031) no-music capture (post-V21-pitch-fix) adds an upstream timeline-driver class: Godot fires its two early `play_sound` calls 24 cadences early (cad 88 vs 112), binds only one pool pair per call (pair 0–1 never bound → V16/V17 silent), and never fires the third call at cad ≈ 646, leaving the layer-3 per-slot probes ~20% under-fired. The 2026-05-19 sibling flare amplitude-deficit doc (`FLARE_VOICE_18_AMPLITUDE_DEFICIT`) isolates V18 (pool slot 2) as the one flare voice whose synthesis inputs pair bit-exact yet whose render is 3.2× quieter than PCSX (mean_abs 0.198 vs 0.637, cos_dist 0.283, cos_dist_phase 0.527), localizing the defect to the SPU-mixer stage: the per-voice vol registers hold 0/0 with no explicit rewrite between KON and KOFF, PCSX alone shows an active mode-1 vol LFO on the channel's subslot 1 (depth 256) with no Godot seed/advance rows for slot 2, and the two voice-event dumpers disagree on `fmod` semantics — unresolved at the doc's date, with the subslot-1 LFO seed the leading suspect. The 2026-05-24 haste breakdown doc (post-wf_idx=4-sawtooth-fix and post-position-invariant-call_index-fix) classifies the remaining `haste_no_music` divergence: voice 21's raw_pitch (150/186 paired IRQs, cos_dist bimodal 0.03–0.66) is the real remaining audio-impactful gap; voice 20's env_vol (169 rows) is audio-irrelevant (silent FM driver); and the audible slots' `pre_pitch_hi` rows are probe-ordering offset (the lfo_handler probe emit since relocated to the BOTTOM of `cadence_body`, post-dispatch, matching PCSX BP @ `0x800174C8`) or dormant-slot music-engine residue (see [[Dormant Slot Residue]]). The 2026-05-24 haste KEYON-LAG follow-up (§13) refines the post-seed-fix residual: voice-20 KEYON and sample-0 SPU state now align, and the remaining carrier phase error comes from the pair-0 bytecode starting ~47 cadences late in the cadence stream — a start-position offset, not a per-cadence dispatch-rate difference. The 2026-05-17 ice (E024) doc adds the mid-playback FMod arming: V21's `Chan::FMod` flips 0→1 on the pair chain's second 0xB2 dispatch (80/20 fmod split in the dense capture), and PCSX's cad-102/108/115 KOFF pulses from per-channel EndBar 0x90 opcodes cut V21 to 81,628 frames vs pre-fix Godot's 169,351. The 2026-05-19 ICE V16 first-note doc localizes the residual `ice_no_music` V16 divergence to a single [4000:6000]-sample half-cycle phase window — a second-order effect of the +2-cadence first-burst dispatch drift on the pre-Note attack/sustain, not the Note commit itself (see [[Effect Sound Timing]]) — with the `cos_dist` run-to-run spread explained by STFT framing straddling that window. The 2026-05-21 ICE V21 phase-drift investigation closes the elevated `ice_no_music` V21 cos_dist (median 0.0383, 5 post-refactor runs) as a measurement artifact, not a parity defect: PCSX-Redux's one-sided audio throttle makes the per-cadence capture rate non-constant (mean 233.571 samples/cad), per-side audible-window trimming leaves the per-voice WAVs 4–7% different in duration, and over V21's only long-playback voice (~2.4 s of Repeat–Coda–EndBar looped arpeggio, 56 Notes / 562 cadences) that accumulates ~100–200 ms of wall-clock phase drift that the STFT cos_dist scorer reads as divergence — with the dispatcher proven bit-exact in the cadence domain (107/107 event lockstep, both KONs' SPU state identical) and the throttle mechanism pinned in `psxcounters.cc` (see [[PCSX-Redux Capture Rig]]). The 2026-05-24 PROBE_PAIRING_ITERATION_ORDER_FIX doc adds the pairing-side view: re-pairing `pre_pitch_hi` by (slot, cadence) instead of call_index confirms the audible slots 2/3/4/5 keep 20–59% real divergence (119/122/43/108 rows) — a writer-path issue open for a writer-tracking probe, ~one-tenth of the mis-pair count. The 2026-05-17 `reraise_no_music` pitch-formula bisection (`probe_pitch_formula_stages`) proves the pitch math bit-exact at all five stages (437/433 rows, cad=1 trace-artifact deficit only) on top of the already bit-exact vol formula — neither FFT math pipeline drives voice_21's chronic cos_dist 0.384 / voice_19 rms_spec_err 0.034 residual; and voice_20's onset sits ~2.6 s late on Godot (first audible sample 115,628 vs 167 on PCSX, pairing at cos_dist 0.0094 once aligned). The 2026-05-17 vol-formula bisection sibling (`probe_vol_formula_stages`) proves the vol math bit-exact on reraise voice_21 at all four stages, attributing the 50+ `probe_vol_inputs`/`probe_vol_lr_staging` row diff to a row-count/iteration-set divergence (Godot emits 2 of PCSX's 4 active channels per drain pass under the `CHAN1_VOL_PRESTAGE` gate) rather than math. The 2026-05-15 protect residual-deficit doc adds a mix-level phase class on `protect_no_music`: post-6-opcode-fix `full_mix` cos_dist 0.090 → 0.155 (cos_dist_phase 0.630 → 0.6815) despite per-voice improvements, fingerprinted as inter-voice phase misalignment driven by the missing per-tick mode-2 pan-LFO (chan_8a) update on voice 21 (see [[LFO Sub-Slot 2 Pan LFO]]). The 2026-05-16 reraise voice-18/19 doc (`RERAISE_VOICE_18_19_CHAN_92_PER_VOICE_OVERRIDE`) adds three reraise residuals: a uniform 1.139× `vol_register` inflation on voices 18+19 from the single-scalar `chan_92` seed (the seed now flows per-sound-id via the FEDS port), a voice-19 pan-LFO (0xED, triangle) swinging between L/R extremes where PCSX holds steady, and voice 21's 0.426 `cos_dist` surviving with every sequencer-side probe clean — residual localized to the C++ SPU mixer stage. The 2026-05-16 reraise root-cause doc (`RERAISE_VOICE_18_PERIODIC_KEYON_MISSING`) records the session's first baseline: voice 18's 57-vs-2 KEYON deficit at Δ=10 cadences (voice_18 cos_dist 0.811, full_mix 0.365) from the missing periodic re-KEY fire in the Godot port — since closed by the `0x600`-gated `idle_timeout` drain fire (see [[KON KOFF IRQ Phasing]]). The 2026-05-17 reraise voice-21 rendered-audio doc (`RERAISE_VOICE_21_RENDERED_AUDIO_DIVERGENCE_WITH_BIT_IDENTICAL_REGISTER_WRITES`) measures the residual with the full per-voice control surface — pitch 169/169, pitch inputs 169/169, vol 167/167, sample start/repeat 2/2, ADSR1 2/2, ADSR2_low 4/4, KON/KOFF mask 5/5, KEYON/KOFF sequencer events at identical cadences — voice_21 is still the session's dominant outlier (cos_dist 0.4207; removing it lands full_mix at the ≤0.02 band), the divergence sits at or below the register layer, and three concrete residuals are pinned: a 194 ms onset gap (0.278 s vs 0.472 s) despite same-cadence KEYON, no descending sweep in either side's pitch-register stream (8927/8157 alternating every ~50 cadences, second note 16314 ×1), and the second note rendering ~3800 Hz on PCSX vs Godot's clean 4180 Hz (= 16314<<4 expected) with PCSX's effective loop stretched to 52–55 output samples vs 42.2 expected — root-caused by the 2026-05-17 FMod fix doc (`V21_FMOD_PITCH_MODULATION_FIX`): PCSX ran V21 with `Chan::FMod=1` throughout (V20 as the FMod=2 source; V20's slowly-varying near-DC modulator averages slightly negative, scaling V21's effective sinc to ~91%), and arming the pair in Godot's mixer collapsed voice_21 cos_dist 0.418 → 0.0015 and full_mix 0.144 → 0.012. The 2026-05-15 SPU event-stream replay doc (`SPU_EVENT_STREAM_REPLAY_PLAN`) adds a third-SPU data point on `protect_no_music`: on the identical event stream, the Mednafen replay and Godot's native mixer both diverge from live PCSX by similar margins (full_mix cos_dist 0.1401 vs 0.1044), corroborating that PCSX's SPU render is the outlier (see [[SPU Event Stream Replay]]). The 2026-05-17 reraise pan-LFO doc (`VOICE_19_RMS_CHAN_8A_PAN_LFO_DIVERGENCE`) pins voice_19's session-top `rms_spec_err` (0.0338) as L/R energy distribution, not amplitude (`mean_abs` pairs 0.099/0.097, `cos_dist_phase` 0.808): the silent drivers' savestate-pre-armed mode-2 pan-LFOs — absent on Godot until the `_apply_lfo_state_seed` seed — commit L/R volume last-write-wins on the shared SPU voice, sweeping the pan where Godot's static silent drivers held it; the same mechanism compounds voice_21's chronic `cos_dist` 0.384 with its audible-driver square-wave pitch-LFO (wf_idx=0), and a separate voice_19 first-KON shift (SPU sample 810 vs 34128) remains open. The 2026-05-24 VOICE_20_ENV_VOL_DECAY_SUSTAIN_DIVERGENCE doc characterizes the post-lfo-relocation voice-20 `env_vol` residual (201/408 rows, all in Decay state): PCSX drops then holds at 6975 (0x1B3F) while Godot decays toward 0, repeating every ~22-cadence re-key; the voice-20 ADSR1/ADSR2 writes and the SPU-level sustain/decay rates are bit-identical on both sides, the per-mix-call step (~32 env/call) matches the SUSTAIN rate and scales linearly with the capture's `samples_per_sub` (pinning the canonical 183 samples/cadence does not shrink the divergence), and a high-frequency trace (BP @ `0x80014660`) shows voice 20's env held at 32767 for 75 cadences and at 6975/0 between key-on cycles — PCSX's SPU mix() is not running every sample for that voice (gating mechanism unresolved at the doc's date).

## Points

- **`cure_no_music` `voice_21` audio still diverges from the PCSX capture at cos_dist ≈ 0.45 and is not a scaled-or-shifted variant of it (best cross-correlation −91 at +792 samples vs +52 at zero shift; least-squares gain −0.0225): the 2026-05-12 run shows matching peaks (PCSX 0.5511 vs Godot 0.5517) with the dominant frequency off by ~40 Hz / ~37 cents (PCSX 1832 Hz vs Godot 1872.5 Hz) — a per-sample pitch-process difference, not a gain or alignment bug.** — `[D·R] 2/3`
  - D: `audio_diff_report.py` + cross-correlation run on the `voice_21` WAV pair (commit ba7acba0; 2026-05-12)
  - D: `MIXER_AUDIO_DIVERGENCE.md` `audio_diff_report.py` re-run on the same `voice_21` pair (commit ba7acba0; 2026-05-12) — top RMS-diff windows cluster in 100–1050 ms (post-onset cadences 24–252), peak spectral diff band 1000–2000 Hz (cure tone fundamental), peak per-sample diff 0.58
  - R: `smd-player/src/shared/` native SPU mixer (`fft_spu_core_runtime.cpp`, `fft_spu_pitch_runtime.cpp` et al.)
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_FINAL_STATE.md`
  - src: `research/effect_sound/working_documents/MIXER_AUDIO_DIVERGENCE.md`
- **Godot fires the pitch-register writes one cadence (~4.17 ms / 184 samples) EARLIER than PCSX starting from the 2nd write, bit-exact on value (voice_21: 165/165 writes value-paired; row 0 matches exactly, rows 1..164 all `diff_cad = −1` — cadence sequences PCSX `1,3,6,8,…` vs Godot `1,2,5,7,…`); with the cure tone's pitch register oscillating aggressively between ~6800 and ~3050 under LFO/vibrato, the 1-cadence shift puts the two sides in different LFO phases at every transition.** — `[D·R] 2/3`
  - D: `probe_pitch_register` voice_21 capture (2026-05-12)
  - R: pitch staging/walker path `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd` + `smd-player/src/shared/fft_spu_voice_runtime.h` (`FFTPitchUpdateScheduled` pending queue); validated by the `probe_pitch_register` pair
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_FINAL_STATE.md`
  - ⚠ SUPERSEDED (2026-08-20) by: SPU register write cadence (ADSR2_LOW, PITCH, VOL_LR_RAW, ADSR2_HIGH) is FFT-faithful post-walker-flag fix — probe_pitch_inputs/_staging/_register all pair with no row-count or value diff
- **Godot's effect-sound LFO runs entirely in the GDScript engine (`channel.lfo_*` → `advance_lfo` → `pitch_bend` → pitch staging → SPU pitch register); the C++ `FFTPitchLfoBlock` machinery (`fft_spu_lfo_tools.cpp`, `mixer.init_voice_pitch_lfo`) is only reachable from the DAW/VST3 build, so `lfo_blocks[0].enabled == false` for all effect voices and `fft_effective_voice_sinc()` is a no-op on that path.** — `[R] 1/3`
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/advance_lfo.gd` (GDScript LFO engine) + `smd-player/addons/exmateria_sound/runtime/sequencer/opcodes/pitch_lfo_init.gd` (Pass 7.D.d — Godot no longer calls `mixer.init_voice_pitch_lfo`; the C++ engine stays in `smd-player/src/shared/fft_spu_lfo_tools.cpp` for the VST3 build)
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_FINAL_STATE.md`
- **The `cure_no_music` playback timeline (cadence = SPU IRQ tick): cadence 0 dispatches both the silent driver Note (voice 20, `delta_time = 4`) and the audible Note (voice 21, `delta_time = 144` — the long first cure tone); the first KON v20+v21 lands at cadence 1 (registers seeded via the 0x1FF walker mask); KOFF v20 at cadence 349 (silent driver pre-Rest release, fired by the idle-timeout drain) and again at 351 when the Rest opcode dispatches; KOFF v21 at 359; the next note's KON is off-by-one at 361 (Godot) / 362 (PCSX).** — `[D·R] 2/3`
  - D: probe-derived timeline (2026-05-11/12; commit fbfe2830 — idle_timeout drain fires KOFF, resolving the cad 349/359 directional inversions)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd` + note-handler idle-timeout drain (KOFF on drain); validated by the `probe_kon_koff_mask` / `probe_pitch_register` pairs
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_FINAL_STATE.md`
- **As of the 2026-05-12 probe state the main remaining structural divergence on `cure_no_music` is the walker flag word entry count: PCSX fires 320 per-slot fan-out entries (`s1 != 0`) vs Godot's 270 — Godot never enters the walker on the 50 ticks where FFT enters with a pure ADSR2_LOW (0x080) flag word, and fires `_fan_vol_lr_raw` alone (0x001, 48 entries) where FFT fires the ADSR2_LOW companion (0x081, 24 entries); the ~70 missing ADSR2-low re-commits leave the voice-21 release-envelope shape divergent (full_mix cos_dist ~0.38, voice_21 ~0.47 vs the 0.09 parity threshold).** — `[D·R] 2/3`
  - D: `probe_walker_flag_word_entry` 320/270 bucket distribution + 7-second audio-score window (2026-05-12)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/spu_irq_walker.gd` + `runtime/shared/dispatcher.gd` (walker-entry and `_fan_vol_lr_raw` cadence) — the gap is tracked as the unpaired `probe_walker_flag_word_entry` in `smd-player/workspace/orchestrator/probe_validation_manifest.py`
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_NEXT_STEPS.md`
  - ⚠ SUPERSEDED (2026-08-20) by: post end-of-note ADSR2_LOW fix (commit 9bf658cc) the walker flag word entry gap is closed — probe_walker_flag_word_entry pairs 320/320 within ±1 drift (318/320 bit-exact, 2 init-seed row diffs remain)
- **Pitch is end-to-end FFT-faithful on `cure_no_music`: `probe_pitch_inputs`, `probe_pitch_staging`, and `probe_pitch_register` all pair 222/222 with no row-count or value diff — SPU pitch register writes (voice + pitch) match end-to-end, ruling out pitch encoding/staging as the source of the remaining audio divergence.** — `[D·R] 2/3`
  - D: cure_no_music Layer 5 pitch probe pairs, 222/222 each (2026-05-12)
  - D: post-walker-flag-fix re-pair — `probe_pitch_inputs`/`_staging`/`_register` all pair with no row-count or value diff, write cadence FFT-faithful ("full pitch pipeline FFT-faithful", 2026-05-16)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/pitch_staging.gd` + `runtime/shared/spu_irq_walker.gd` (`_fan_pitch`) — validated by the `probe_pitch_register` pair
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_NEXT_STEPS.md`
- **Post end-of-note ADSR2_LOW write fix (commit 9bf658cc — 74 added walker passes committing `_fan_adsr2_low`), `probe_walker_flag_word_entry` pairs 320/320 within ±1 drift on `cure_no_music` (318/320 bit-exact, 2 init-seed row diffs) and the audio baseline improved ~0.07 on full_mix (0.38 → 0.31) and ~0.10 on voice_21 (0.47 → 0.37) — the end-of-note ADSR2_LOW write was the single biggest mover.** — `[D·R] 2/3`
  - D: `probe_walker_flag_word_entry` pair + `run_effect_iteration.py --session cure_no_music` audio-score baseline, post-9bf658cc (2026-05-16)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd` (KOFF-prep gate sourcing the 74 walker passes) + `runtime/shared/spu_irq_walker.gd::_fan_adsr2_low`
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_NEXT_STEPS_V2.md`
- **The play_sound init seed sets the walker flag word to 0x1FF on PCSX — arming every arm, including ADSR1 (0x10/0x20/0x100), SAMPLE_ADDR (0x8), and VOL_LR_SWEEP (0x2) — while Godot's init seeds only 0xC5, missing those five arms; the two surviving `probe_walker_flag_word_entry` row diffs are exactly these init-seed entries.** — `[D·R] 2/3`
  - ⚠ SUPERSEDED (2026-08-20) by: on PCSX the 0x1FF walker arm is written only by the 0xAC AC opcode's data loader (`Hyp_instrument_data_loader`, store at PC `0x80017088`) — the cad=0/1 `cure_no_music` 0x1FF entries are bind-time AC dispatches at savestate load, not an unconditional play_sound init seed
  - D: `probe_walker_flag_word_entry` 2 init-seed row diffs, PCSX 0x1FF vs Godot 0xC5 (2026-05-16)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/play_sound.gd` init-seed path — the doc names Godot's init as skipping the missing arms (observed 0xC5 seed in the Godot capture)
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_NEXT_STEPS_V2.md`
- **`probe_slur_propagation_pre` shows PCSX takes 2 more per-channel walker exits than Godot on `cure_no_music` (81 vs 79) via the `chan_word_0 & 0x500 != 0` exit condition — a pre-existing structural diff.** — `[D·R] 2/3`
  - D: `probe_slur_propagation_pre` capture (2026-05-16)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd` (walker-exit loop condition mirror on `channel_word_0 & 0x500`)
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_NEXT_STEPS_V2.md`
- **Godot fires `_fan_vol_lr_raw` 2× more than FFT at voice 21's ramp start on `cure_no_music` (`probe_vol_register` 122/124), with values bit-exact when aligned — the drift is in walker-flag timing, not vol logic.** — `[D·R] 2/3`
  - D: `probe_vol_register` capture (2026-05-16)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/spu_irq_walker.gd::_fan_vol_lr_raw`
  - src: `research/effect_sound/working_documents/AUDIO_PARITY_NEXT_STEPS_V2.md`
- **Death's (E030) 3rd `play_sound` (cad 254) re-allocates pair slot 4 with no preemption — `killed_kon_mask=0`/`killed_mask=0` because the V20/V21 pair already released at the cad-191 EndBar — and re-binds it with global-bank bytecode dispatching from cad 256 and re-KEYONing at cad 257, producing the second audible burst of `spu_voice_21.wav` in [1.24 s, 2.40 s] (PCSX 105,835 frames vs Godot 54,725; the first 0.91 s matched within ±7% RMS) — Godot truncated V21 at 1.24 s until the catalog replay fired the equivalent trigger.** — `[D·R] 2/3`
  - D: `probe_play_sound_alloc` (call 3: slot=4, killed_kon_mask=0, killed_mask=0), `probe_kon_koff_mask` (PCSX-only rows at cad 255/257/404/407), and the `spu_voice_21.wav` pair (RMS ratio 0.00 after 1,133 ms on Godot), death_no_music captures (2026-05-18)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/pool.gd` `find_free_pair_slot` (pass 1 free-slot scan, pass 2b preempt scan — LRU proxy for FFT's entity+0x10 preempt priority, PC 0x80012E08–0x80012E70) + `runtime/effect_sound/play_sound.gd` `play_feds_pair` slot re-bind; validated by the post-fix death_no_music orchestrator re-run (probe_play_sound_call 2/2, KON pair_rate 0.7241 → 1.0000)
  - src: `research/effect_sound/working_documents/DEATH_V21_MISSING_THIRD_PLAY_SOUND_DEFICIT.md`
- **Pre-fix, death parity ran in reverse: the orchestrator's NOP-patch zeroed the JAL at `0x8006B97C` in death's RAM image so PCSX matched Godot's V21 gap instead of the other way around — with the patch death PAIRs 2/2 (`voice_21.cos_dist` 0.0619, `pair_rate` 1.0), while unpatched, Godot's missing third call leaves `voice_21.cos_dist` at 0.2576 vs ~0.05 on death's other voices.** — `[S·D] 2/3`
  - S: `0x8006B97C` — the death BATTLE.BIN trigger JAL site, zeroed by the orchestrator's `BATTLE_BIN_SFX_NOPS` NOP-patch, per `research/effect_sound/working_documents/GLOBAL_SFX_BANK_INTEGRATION_PLAN.md` §1/§4 Step 6
  - D: `death_no_music` pre-fix baseline runs (2026-05-18) — `audio_score.json` `voice_21.cos_dist` 0.2576 (NOP-patch off) vs 0.0619 (NOP-patch on); `report.md` `pair_rate` 1.0
  - R: none — the NOP-patch reverse-parity state is a research/ effect_alignment orchestrator artifact (`effect_capture_orchestrator.lua`), not present in godot-learning/smd-player/fft-sound-driver/effect-editor
  - src: `research/effect_sound/working_documents/GLOBAL_SFX_BANK_INTEGRATION_PLAN.md`

- **The env_vol divergence on `probe_envelope_tail` is confined to the SPU mixer's per-sample env_vol evolution, not chan-side dispatch: ADSR1+ADSR2 register writes are bit-identical PCSX↔Godot across all seven sessions (haste 372/1510, cure 1/358, ice 712/1696, protect 196/723, cure_4 799/2383, reraise 968/2505, zombie 431/2044 divergent rows).** — `[D] 1/3`
  - D: `probe_envelope_tail` env-tail divergence counts (7 sessions) + commit `4c4c19ab` voice-20 env trajectory (2026-05-24)
  - D: voice-20 env_vol observation — PCSX 6975 vs Godot various (e.g. 14303), Δ ≈ 7328 (2026-05-24)
  - R: none — per-sample env_vol mixer evolution not present in godot-learning (probed godot-learning/src, godot-learning/tests; the native mixer lives in smd-player/src/shared)
  - src: `research/effect_sound/working_documents/ENV_VOL_ARCHITECTURAL_GATING_PCSX_ADPCM_STOP.md`
  - src: `research/effect_sound/working_documents/ENV_VOL_WITHIN_IRQ_KEY_ON_TIMING.md`
- **Godot's KEY-ON commit fires at a fixed sample offset within each sub-tick, in contrast to PCSX's wall-clock-variable offset: as of 2026-05-24 the per-IRQ render loop rendered `n_pre = samples_per_sub − POST_DISPATCH_TAIL_SAMPLES` (32) samples, then fired the dispatcher (the KEY-ON commit), then rendered the 32-sample post-tail — so KEY-ON landed at sample 151 of the canonical 183 (258 for the calibrated 290 of the haste run); the pre/post-dispatch split has since been removed (Pass 8 Phase 7 — `rt.tick()` runs the whole per-IRQ pipeline, then one mix chunk), so the KEY-ON now lands at offset 0 of the sub-tick chunk.** — `[D·R] 2/3`
  - D: haste calibration run `samples_per_sub` = 290 (2026-05-24)
  - R: `smd-player/workspace/harness/render_effect_sound.gd` sub-tick loop (`samples_per_sub` default 183 = 44100/240 truncated; `--samples-per-sub` override for PCSX-alignment captures) + `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd` (`FLUSH_PER_DISPATCH = 8`) — validated by the paired-capture full_mix comparison loop (`smd-player/workspace/orchestrator/run_effect_iteration.py`)
  - src: `research/effect_sound/working_documents/ENV_VOL_WITHIN_IRQ_KEY_ON_TIMING.md`
- **The PCSX KEY-ON commit lands at a variable sample offset within the cadence: the SPU thread mixes at 44.1 kHz in real time in parallel with the MIPS IRQ, so by the time the `FUN_8001ACF0` epilogue commit fires the SPU has already mixed N samples (N = wall-clock-elapsed × 44.1 kHz) — N grows under Lua probe load (`samples_per_cadence` observed 190–320 in `cadence_calibration.json`, vs canonical 183) — so the post-KEY-ON mix-call count per cadence varies run-to-run and a fixed Godot in-sub-tick offset can only approximate PCSX.** — `[D·R] 2/3`
  - D: `cadence_calibration.json` `samples_per_cadence` 190–320 vs canonical 183 (2026-05-24)
  - R: `smd-player/workspace/harness/render_effect_sound.gd` (`--samples-per-sub` override driven by `probe_cadence_wallclock.jsonl` aligns the Godot WAV timeline to the measured PCSX pace — the reimplementation-side response to this variable pacing) — validated by the paired-capture full_mix RMS/cos_dist comparison (`smd-player/workspace/orchestrator/run_effect_iteration.py`)
  - src: `research/effect_sound/working_documents/ENV_VOL_WITHIN_IRQ_KEY_ON_TIMING.md`
- **The residual `probe_kon_koff_mask` FAIL lines on disillusionment_3 (167/169) and zombie (11/10) are NOT slot-allocator routing mismatches but CADENCE DRIFT beyond the audio validator's ±2-cadence pairing tolerance: per-voice lines show events firing on BOTH sides (disillusionment_3 voice_18: KON 56/56, KOFF 58/56), mean drift ≈ 11 ms, and the audio cos_dist on those voices is < 0.014 — i.e. the probe FAIL is a pairing-tolerance artifact, not a dataflow or audio issue.** — `[D] 1/3`
  - D: last-run `probe_kon_koff_mask` + `audio_score` on disillusionment_3_no_music / zombie_no_music (per-voice KOFF/KON counts, mean drift, cos_dist < 0.014) (2026-05-19)
  - R: none — the ±2-cadence pairing tolerance lives in the orchestrator's `audio_score` validation (`smd-player/workspace/orchestrator/`), not in godot-learning/smd-player runtime (probed: no pairing-tolerance code in `godot-learning` or `smd-player`)
  - src: `research/effect_sound/working_documents/FFT_GODOT_DATA_FLOW_ALIGNMENT.md`
- **As of 2026-05-19 (post-V21-pitch-fix) the Godot flare (E031) timeline driver diverges from PCSX on three fronts at once: the first two `play_sound` calls fire 24 cadences early (cad 88 vs 112 — within the same ~100 ms window, anchored at the first opcode dispatch on both sides), each call binds only one pool pair (pairs 4–5 then 2–3; pair 0–1 is never bound, so voices 16/17 stay silent at cos_dist 1.0), and the third call at cad ≈ 646 never fires — the layer-3 per-slot probes under-fire ~20% (`probe_per_channel_tick_entry` 2184 vs 2712), the shadow of the two unbound pairs plus the `channel_word_0 != 0` inactive-slot gate; raw cadence rate itself pairs (`probe_cadence_source`/`probe_cadence_wallclock` 1680 = 1680), so the drift is anchor-placement, not tick rate.** — `[D·R] 2/3`
  - D: `flare_no_music` capture post-Candidate-A: `probe_play_sound_call` 2 vs 3, `probe_slot2_init_state` 16 vs 24 rows, `probe_per_channel_tick_entry` 2184 vs 2712, `probe_lfo_handler_entry` 2188 vs 2720, `probe_smd_interpreter_tick_entry` 2188 vs 2717, KON 8 vs 10, KOFF 11 vs 18, voice_16/voice_17 cos_dist 1.0 (2026-05-19)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound_controller.gd` (`_fire_initial_keyframes` / `_tick_channel` / `_maybe_fire` keyframe elapse) + `runtime/effect_sound/play_sound.gd::play_feds_pair` (single-pair bind per call) — validated by the `flare_no_music` orchestrator probe-pair counts (`smd-player/workspace/orchestrator/run_effect_iteration.py --session flare_no_music`), which currently FAIL on the counts above
  - src: `research/effect_sound/working_documents/FLARE_SLOT_INIT_DRIFT_DEFICIT.md`
- **On flare (E031), every probed FFT-side synthesis input for V18 (pool slot 2) pairs bit-exact PCSX↔Godot in the `flare_no_music` capture — the two KONs at cadences 1 and 242 (`mask_full` 0x3C0000: PCSX KONs voices 18–21 as a 4-voice block in a single SPU write, callsite 0x80014AC4; Godot emits one row per voice from the same `_walker` fan-out tick), the ADSR registers (adsr1_low/mid nibble 15, adsr1_high attack_rate 0 / mode_bit 0, adsr2_low 24518), the sample start/repeat addrs (4864→449104 and 784→445024 at cadence 1; raw 449104 / 445024 at cadence 242), the per-cadence pitch-register sweep (255, 259, 264, 269, … value-exact at matching cadence/voice positions), the vol-formula inputs (chan_88/8a 0, chan_90 16384, chan_92 13248 after the PCSX 0→11136→13248 init sweep, pool_96 32512), and the per-sample envelope progression (env_vol 448→896→1023, sustain capped at 1023, adsr_state 0→1→2 attack→decay→sustain) — bounding the V18 divergence to the SPU-mixer stage between register writes and PCM emission.** — `[D·R] 2/3`
  - D: `probe_keyon_per_voice.jsonl` (2 rows), `probe_pitch_register`/`probe_pitch_inputs`/`probe_pitch_staging` (261 rows each), `probe_vol_register_sweep`, `probe_adsr1_{low,mid,high}_register`, `probe_adsr2_low_register`, `probe_sample_start_addr_register`, `probe_sample_repeat_addr_register`, `probe_envelope_tail.jsonl` (676 rows), both sides' `spu_voice_events.jsonl` (6267 PCSX rows), the Godot `vol_inputs` trace row — `flare_no_music` last_run capture (2026-05-19)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/play_sound.gd` (Godot-side `keyon_per_voice` / `vol_inputs` probe emission, L1019-1025 / L1632-1639) + `runtime/shared/spu_irq_walker.gd` (per-voice fan-out) — validated by the `flare_no_music` orchestrator run (`smd-player/workspace/orchestrator/run_effect_iteration.py --session flare_no_music`), which pairs the probes above
  - src: `research/effect_sound/working_documents/FLARE_VOICE_18_AMPLITUDE_DEFICIT.md`
- **FFT never explicitly re-writes V18's per-voice volume registers between its two flare KONs and the KOFF: the vol-register sweep fires exactly twice (cadence 1 and cadence 242) on each side, both writing (vol_l=0, vol_r=0) — so the SPU's per-voice `vol_l`/`vol_r` simply hold what the per-tick vol prestage committed (the `FUN_80017118` walker dispatches the vol fan-out; vol-formula stage at PC `0x800171F4`, `lh v0, 0x92(s0)`).** — `[S·D·R] 3/3`
  - S: `0x80017118` (FUN_80017118 / `fft_walker_per_channel` — per-channel walker dispatching the pitch/vol/adsr1/adsr2/sample-addr SPU register writes; vol-formula stages 0x800171F4–0x80017248) — doc §8.2, cross-referenced by `fft-ghidra/content/labels_scus.tsv` and `research/key_documents/master_parser.py:3039`
  - D: `probe_vol_register_sweep.jsonl` (2 rows, both (0,0)) + Godot `vol_inputs` trace ({chan_88:0, chan_8a:0, chan_90:16384, chan_92:13248, chan_98:0, pool_96:32512, pool_ae:0}) + PCSX `diag_chan_92_writers.jsonl` (transient 0→11136→13248 init sweep ending at 13248) — `flare_no_music` (2026-05-19)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/spu_irq_walker.gd::_fan_vol_lr_raw` (per-voice vol-register fan-out, FUN_8001B428 entry-cadence mirror) + `runtime/shared/note_handler/compute_vol_lr.gd` — validated by the flare_no_music `probe_vol_register_sweep` 2/2 pair
  - src: `research/effect_sound/working_documents/FLARE_VOICE_18_AMPLITUDE_DEFICIT.md`
- **Godot's flare V18 render is 3.2× quieter than PCSX despite the fully-paired synthesis inputs: mean_abs 0.198117 vs 0.637488 (−68.9%), cos_dist 0.282653 (FAIL vs the 0.05 PAIR threshold), cos_dist_phase 0.527163 (substantially larger than cos_dist, so the waveform is temporally shifted, not just scaled), while onsets pair within 18 samples (897 vs 915, ≈0.4 ms) and the ADSR/envelope pair — the dominant open defect in the flare session at the doc's date.** — `[D·R] 2/3`
  - D: `audio_score.json` V18 metrics (mean_abs, cos_dist, cos_dist_phase, rms_spec_err 0.033828, onset_samples 897/915, kon 2/2) — `flare_no_music` last_run (2026-05-19)
  - R: `smd-player/src/native/fft_spu_mixer_native.cpp` (the C++ SPU mixer that produces `spu_voice_18.wav`) — the flare_no_music orchestrator run still FAILs on V18 cos_dist 0.283 > 0.05
  - src: `research/effect_sound/working_documents/FLARE_VOICE_18_AMPLITUDE_DEFICIT.md`
- **PCSX's flare V18 channel (pool slot 2, chan_base 0x80037458) carries an active mode-1 vol LFO in subslot 1, seeded from the entity savestate at cadence 0 (mode=1, depth 256 of 256, depth_reload 256, countdown 25, inner_reload 30, step_current 27962026, step_source −27962026, accumulator −671088624 — non-zero from the start) across 340 probe rows, while Godot's `effect_sound_trace.jsonl` has no `lfo_subslot1`-equivalent rows for slot 2 in the sampled window and Godot's V18 `left_volume`/`right_volume` stay 0/0 through the entire sustain even though audio is being rendered.** — `[D·R] 2/3`
  - D: `probe_lfo_subslot1_state.jsonl` (340 rows, chan_base 0x80037458, mode=1 / depth=256) + Godot `spu_voice_events.jsonl` left_volume/right_volume=0 rows + `effect_sound_trace.jsonl` (no slot-2 lfo_subslot1 rows in window) — `flare_no_music` (2026-05-19)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/play_sound.gd::_apply_lfo_state_seed` (L572 — copies savestate LFO fields onto ChannelState; the doc's Candidate-A audit site) + `runtime/shared/per_tick/advance_lfo.gd` (per-cadence LFO advance) — validated by the flare_no_music run; subslot-1 seed coverage for slot 2 unconfirmed (doc §7 step 3)
  - src: `research/effect_sound/working_documents/FLARE_VOICE_18_AMPLITUDE_DEFICIT.md`
- **The two voice-event dumpers disagree on the `fmod` field for flare V18: Godot's `spu_voice_events.jsonl` writes `fmod:0` on the pre-KON row and `fmod:2` on every subsequent row, while PCSX's capture has no `fmod` column at all for V18 — and since the SPU's per-voice fmod-enable is a single bit packed into the 24-bit register, `fmod:2` cannot be a raw register value; the Godot dumper's field semantics (chain-role code vs enable-bit vs sentinel) were unresolved at the doc's date.** — `[D·R] 2/3`
  - D: `spu_voice_events.jsonl` both sides (Godot fmod 0→2 at KON; PCSX column absent for V18) — `flare_no_music` (2026-05-19)
  - R: `smd-player/src/native/fft_spu_mixer_native.cpp` — event-row writer `row["fmod"] = next.fmod` (L466, row also carries `left_volume` L410 and `decoded_sample` L418) + `set_voice_fmod` binding (L234, the doc's Candidate-C audit site); no named test (open probe-semantics item, doc §7 step 4)
  - src: `research/effect_sound/working_documents/FLARE_VOICE_18_AMPLITUDE_DEFICIT.md`
- **Flare V18 KOFF counts pair (3 PCSX / 3 Godot) but one KOFF cadence is unpaired — 1 missed + 1 extra with `koff_drift_mean_ms` 0.0, i.e. the unpaired events sit beyond the pairer's ±2-cadence window rather than drifting; the doc tracks it as a smaller follow-up orthogonal to the amplitude defect.** — `[D·R] 2/3`
  - D: `audio_score.json` V18 KOFF fields (koff_pcsx 3, koff_godot 3, koff_paired 2, koff_missed 1, koff_extra 1, drift_mean_ms 0.0) — `flare_no_music` (2026-05-19)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/flush_tick.gd::flush_koff_post_loop` (post-loop KOFF commit path) — the flare_no_music run reports the 1 missed + 1 extra KOFF
  - src: `research/effect_sound/working_documents/FLARE_VOICE_18_AMPLITUDE_DEFICIT.md`
- **Haste's SMD pair is FM: SPU voice 20 is the silent FM driver (modulator) and voice 21 the audible FM carrier — so voice 20's env_vol divergence (169 of 197 paired rows, 85% of the env_vol category) is audio-irrelevant: per the doc's reading of PCSX `FModChangeFrequency` semantics the FM modulator's contribution to voice 21 is its sample value, NOT its env-scaled output.** — `[D·R] 2/3`
  - D: `probe_envelope_tail` paired by (call_index, voice), `haste_no_music` capture (2026-05-24)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/opcodes/fmod_enable.gd` (0xB2) + `fmod_disable.gd` (0xB3) + `smd-player/src/shared/fft_spu_mix_tools.cpp` iFMod chain (mirrors PCSX-Redux `spu.cc` `FModChangeFrequency`; fmod==2 source never sums into the master mix) — validated by the `haste_no_music` paired-capture comparison (`smd-player/workspace/orchestrator/run_effect_iteration.py --session haste_no_music`)
  - src: `research/effect_sound/working_documents/HASTE_REAL_VS_INSTRUMENTAL_DIVERGENCE_BREAKDOWN.md`
- **On the audible haste slots (2/3/4/5 = voices 18/19/20/21) `pre_pitch_hi` ramps with IDENTICAL per-IRQ deltas to PCSX (+80, +213, +261, +126) — Godot's pre-fix lfo_handler probe sampled one cadence late: it fired at the TOP of `cadence_body` (BEFORE opcode dispatch updated `pre_pitch_acc_u32` / chan+0x82), while PCSX's BP at `lfo_handler_tick` entry fires AFTER `smd_interpreter_tick` + `per_channel_tick` had already updated chan+0x82 for the current IRQ; the emulation is correct, only the probe snapshot was offset (audio-irrelevant).** — `[S·D·R] 3/3`
  - S: `0x800174C8` — `lfo_handler_tick` per-channel loop entry / PCSX BP site (cited in `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd` comments; `0x800174C8–0x800175E0` per [[Noise LFO PRNG]])
  - D: `probe_chan_pitch_state.pre_pitch_hi`, `haste_no_music` capture (2026-05-24) — slot 3 (voice 19) sample table: PCSX +80/IRQ vs Godot one-shot +12288 catch-up at cad 3, then identical +80/IRQ deltas
  - D (additive): pre-relocation `pre_pitch_hi` false-divergent rows ~390 across the 4 audible slots (§7.1 per-slot: slot 2/voice 18 119, slot 3/voice 19 122, slot 4/voice 20 43, slot 5/voice 21 108) with the +80/+213/+261/+126 per-IRQ deltas in lockstep on both engines and the one-IRQ lag — `probe_chan_pitch_state`, `haste_no_music` (2026-05-24)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd::cadence_body` — the lfo_handler probe emit since relocated to the BOTTOM of `cadence_body`, after opcode dispatch (`PROBE_LFO_HANDLER_RELOCATION_PLAN.md`), matching PCSX BP @ `0x800174C8` — validated by the `probe_chan_pitch_state` pair
  - src: `research/effect_sound/working_documents/HASTE_REAL_VS_INSTRUMENTAL_DIVERGENCE_BREAKDOWN.md`
  - src: `research/effect_sound/working_documents/PROBE_LFO_HANDLER_RELOCATION_PLAN.md` (relocation plan; landed as bottom-of-cadence_body emit, tick() move rejected per dispatcher.gd comment)
- **After the wf_idx=4 sawtooth fix (commit `dc3721f5`) and the position-invariant call_index pairing fix (commit `c8c03696`), the largest audio-impactful haste divergence is voice 21's (audible FM carrier) raw_pitch: 150 of 186 paired IRQs diverge, cos_dist bimodal 0.03–0.66 (the source of voice 21's cos_dist 0.43); audible-channel env_vol contributes ~60 rows total (voice 18: 22, voice 19: 15, voice 21: 24) and fmod only 4 rows (3 + 1), so fmod is not a primary driver.** — `[D·R] 2/3`
  - D: `probe_envelope_tail` / raw_pitch paired rows, `haste_no_music` post-fix capture (2026-05-24)
  - D (additive): the post-both-fixes per-voice raw_pitch table (same capture) — v18 57/60 (median Δ sub-quarter-tone), v19 191/230, v20 12/197, v21 150/186 (81%); voice 21 cos_dist 0.427, full_mix 0.011; the LFO subslot seed fix moved voice 20 (0.075→0.045) without moving voice 21, localizing the residual to voice-21-local pitch inputs
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/advance_lfo.gd` (per-cadence LFO advance, wf_idx branches) + `runtime/shared/per_tick/pitch_staging.gd` (voice 21 pitch write path) — validated by the `haste_no_music` paired-capture comparison (`smd-player/workspace/orchestrator/diff_voice21_pitch.py`, `run_effect_iteration.py`), which still exposes the gap
  - src: `research/effect_sound/working_documents/HASTE_REAL_VS_INSTRUMENTAL_DIVERGENCE_BREAKDOWN.md`
  - src: `research/effect_sound/working_documents/VOICE_21_RAW_PITCH_DIVERGENCE_INVESTIGATION.md`
- **Voice 19's haste D9 pitch LFO is wf_idx 4: FFT dispatches `PTR_LAB_80028F54[4]` to `LAB_800177C0` — the sawtooth-with-reset boundary (accumulator reset to 0, countdown reloads) — not the swap-with-negation default; Godot's pre-fix fall-through produced ±step alternation instead of sawtooth-with-reset (raw_pitch max delta 5664 at cadences 328–333), and the wf_idx 4/5 branch (commit `dc3721f5`) fixed it; the residual is 191 of 230 divergent rows, median |Δ| ~30 SPU pitch units (sub-audible per row, cos_dist 0.013).** — `[S·D·R] 3/3`
  - S: `LAB_800177C0` / `PTR_LAB_80028F54` (LFO callback dispatch table at `ram:80028f54`, dispatch site `0x80016580`) — disasm trace in `research/effect_sound/working_documents/V19_WF_IDX_4_SAWTOOTH_FIX.md` + `advance_lfo.gd` comment citing `scus_disassembly.txt:13742..13747`
  - D: `probe_envelope_tail.raw_pitch` voice 19 pair, `haste_no_music` (2026-05-24) — pre-fix max delta 5664 (cad 328–333); post-fix 191/230 divergent, median |Δ| ~30 units
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/advance_lfo.gd` L118–132 (wf_idx 4/5 → `LAB_800177C0` reset branch; commit `dc3721f5`) — validated by the `haste_no_music` `probe_envelope_tail` pair
  - src: `research/effect_sound/working_documents/HASTE_REAL_VS_INSTRUMENTAL_DIVERGENCE_BREAKDOWN.md`
- **FM amplifies per-sample modulator divergence: the carrier's pitch is scaled by `(32768 + iFMod[ns]) / 32768` every output sample with `iFMod = modulator.sval` (`FModChangeFrequency`), so a ~5% sample-level difference in voice 20 becomes a ~5% pitch deviation in voice 21 on every sample (sidebands, beating, a chorused "wrong instrument" sound) — which is why `haste_no_music` voice 20 (the FM modulator) stays at cos_dist 0.011/0.010/0.016 while voice 21 (the FM carrier) swings 0.399/0.668/0.658 across three runs: a time-averaged cos_dist on the modulator forgives sample-domain jitter, but the carrier's spectrum itself changes, so onset + lag alignment cannot compensate.** — `[D·R] 2/3`
  - D: three-run cos_dist table (voice 18 0.004; voice 19 0.037/0.036/0.028; voice 20 0.011/0.010/0.016; voice 21 0.399/0.668/0.658; full_mix 0.014/0.012/0.011) + `spu_voice_events.jsonl` voice 21 `fmod=1` from sample 12704 through ~86162 with `raw_pitch` oscillating 4096-6888 (FM envelope present on Godot, driven by the wrong modulator phase) — `haste_no_music` (2026-05-23/24)
  - R: `smd-player/src/shared/fft_spu_mix_tools.cpp:173-228` (iFMod chain: the fmod==2 source voice feeds `i_fmod` and is excluded from the SSumL/R mix; the fmod==1 target multiplies `effective_sinc` by `(32768 + i_fmod) / 32768` — mirrors PCSX-Redux `spu.cc:272-289` `FModChangeFrequency`) — validated by the `haste_no_music` paired-capture comparison (`smd-player/workspace/orchestrator/run_effect_iteration.py --session haste_no_music`), which still exposes the gap
  - src: `research/effect_sound/working_documents/HASTE_VOICE_21_FMOD_LFO_RESIDUE_FIX.md`
- **Post-seed-fix on `haste_no_music` the voice-20 KEYON and sample-0 SPU register state align between PCSX and Godot (both engines have the voice playing the haste instrument from ~sample 0–44); the residual voice-21 divergence is in bytecode-driven STATE CHANGES, not a per-cadence dispatch-rate difference — both engines run the same pair=0 bytecode at the same per-cadence rate, but PCSX's trigger fired at animation frame 7 pre-anchor, so by sample 0 its bytecode had already ticked ~47 cadences and sits near the pitch_bend 1042 step (fires at sample 810, cad 2.77 post-trigger), while Godot's seed-fix-only trigger fires at ~cad 56, so the same pitch step (1024→1042) fires at sample 14500 — ~47 cadences later, and subsequent pitch steps drift apart (~110 vs ~556 samples/step).** — `[D·R] 2/3`
  - D: per-voice trajectory comparison from `spu_voice_events.jsonl` (voice 20: on=True at 0/44; env_state attack within 0..3; curr_addr 48912→48928 at sample 104/134; pitch 1024→1042 at sample 810/14500) — haste_no_music post-seed-fix, samples_per_sub=292, 1680 pulses (2026-05-24)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/play_sound.gd` (`play_feds_pair` pair-0 bytecode dispatch — the bytecode starts when the keyframe trigger fires) + `runtime/effect_sound_controller.gd` (keyframe-elapse trigger firing) — validated by the `haste_no_music` paired-capture comparison (`smd-player/workspace/orchestrator/run_effect_iteration.py --session haste_no_music`)
  - src: `research/effect_sound/working_documents/HASTE_VOICE_21_KEYON_LAG_INVESTIGATION.md`
- **Ice's V21 FMod is armed mid-playback, not at note start: PCSX dispatches SMD 0xB2 twice on the V20/V21 pair chain and V21's `Chan::FMod` flips 0→1 partway through playback (dense capture: 139,455 rows, fmod=0 on 111,150 (80%) vs fmod=1 on 28,305 (20%)), and PCSX cuts V21's natural decay with the cadence-102/108/115 KOFF pulses from the per-channel EndBar (0x90) opcodes on the V20/V21 chain — 81,628 frames (~1.85 s) vs the pre-fix Godot's 169,351 frames (~3.84 s), unmodulated and uncut, which drove voice_21 cos_dist to 0.466 before the pool-exhaustion fix.** — `[D·R] 2/3`
  - D: `spu_voice_dense.jsonl` (139,455 V21 rows; 80/20 fmod split) + `probe_opcode_b2_fmod_set.jsonl` (2 dispatches on PCSX vs 1 on pre-fix Godot) + `spu_voice_21.wav` pair (81,628 vs 169,351 frames), `ice_no_music` capture (2026-05-17)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/opcodes/fmod_enable.gd` (0xB2 FMod arm; FFT analog `Hyp_smd_op_test_ch2d_bit0` @ 0x8015ED8) + `fmod_disable.gd` (0xB3) — validated by the post-fix `ice_no_music` `probe_opcode_b2_fmod_set` / opcode-dynamics probe pairs
  - src: `research/effect_sound/working_documents/ICE_ANIMATE_TICK_KEYFRAME_ADVANCE_DEFICIT.md

- **On `ice_no_music` V16 the per-voice WAV divergence is confined to one 2000-sample window [4000:6000] (Godot 14% louder, RMS ratio 1.14; every other window within ±6%): the two carriers sit ~half a cycle apart (≈10 samples) over ~30 samples around sample 5000 (PCSX wave trough vs Godot wave peak) and re-sync by sample 6000; because the first Note's pitch commit lands at WAV sample ~25750 (PCSX) / ~26250 (Godot) at ~250 samples/cadence, the window is NOT the 2-cad Note-dispatch drift itself but its second-order effect on the pre-Note attack/sustain wave phase, and the run-to-run `cos_dist` spread (0.012–0.034) comes from the STFT framing straddling that window differently as the onset detector's lock shifts (`pcsx_onset_samples` 6037–27302 across the 4 runs).** — `[D·R] 2/3`
  - D: `spu_voice_16.wav` 2000-sample-window RMS table (ratio 1.14 at [4000:6000], ≤1.05 elsewhere) + sample-by-sample diff around 5000 + 4-run `voice_16.cos_dist` 0.0273 / 0.0275 / 0.0172 / 0.0119 — `ice_no_music` post-`ac89b462` (2026-05-19)
  - R: `smd-player/src/native/fft_spu_mixer_native.cpp` (per-voice WAV emission through the batched mixer) — exposed by the `ice_no_music` audio-score runs (`smd-player/workspace/orchestrator/run_effect_iteration.py --session ice_no_music`)
  - src: `research/effect_sound/working_documents/ICE_V16_FIRST_NOTE_2_CADENCE_DRIFT_DEFICIT.md`
- **Ice (E024) voice 21's elevated cos_dist (median 0.0383, range 0.0214–0.1999 across the 5 post-`b4394e0d` runs) is a measurement artifact, not a parity defect: PCSX-Redux's non-constant per-cadence capture rate accumulates ~100–200 ms of wall-clock phase drift over v21's ~2.4 s of bytecode-driven playback, the independent per-side audible-window trim bakes that drift into the scored pair, and the STFT cos_dist scorer then compares the misaligned WAVs as if they covered the same time window — the dispatcher, walker, and mixer are all bit-exact at the cadence-domain level.** — `[D] 1/3`
  - D: five `ice_no_music` post-refactor runs (2026-05-21) — per-voice cos_dist table (v21 median 0.0383 / outlier 0.1999 vs pool-slot pair-partner v20 0.0024 and v18 0.0023), per-voice WAV duration deltas 4–7% on ALL voices (v21: PCSX 3.501 s / 154,395 frames vs Godot 3.659 s / 161,373 frames, +4.5%), 107/107 dispatch lockstep
  - R: none — the v21 phase-drift scoring correction is not present in godot-learning (probed godot-learning/src, godot-learning/tests; smd-player's `cos_dist_phase_lag_aligned` in `smd-player/workspace/orchestrator/score_audio.py` is scorer tooling, not a runtime parity fix)
  - src: `research/effect_sound/working_documents/ICE_V21_COS_DIST_PHASE_DRIFT_INVESTIGATION.md`
- **Ice voice 21's bytecode dispatch is in PERFECT lockstep between both sides: all 107 events fire at identical cadence indices (delta_cad = 0 on every row) between PCSX's `probe_event_dispatch` (FFT PC 0x800153A4, chan_base 0x80037878) and Godot's `event_dispatch` trace (slot_idx 5), spanning 562 cadences — ruling out every dispatcher / walker / opcode-timing class of bug for that voice; pair-partner voice 20 (same pool slot 4, same play_feds_pair call, same bind tick) dispatches only 11 events and terminates, isolating playback duration as the sole differentiator.** — `[S·D·R] 3/3`
  - S: FFT PC 0x800153A4 (per-event bytecode dispatch breakpoint site) — `smd-player/workspace/probes/probe_event_dispatch.lua` + `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd::_dispatch` "Mirror of FFT BP @ 0x800153A4" comment
  - D: v21 dispatch stream pairing, 107/107 events, zero cadence drift across 562 cadences (2026-05-21)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd::_dispatch` (event_dispatch emit; cadence counter reset at first fire) + `runtime/shared/opcodes/_table.gd` — validated by `smd-player/workspace/orchestrator/run_effect_iteration.py --session ice_no_music`
  - src: `research/effect_sound/working_documents/ICE_V21_COS_DIST_PHASE_DRIFT_INVESTIGATION.md`
- **Ice voice 21's SPU register state is identical between PCSX and Godot at BOTH key-ons — first KON (cad 1): raw_pitch=1360, start_addr=4256, loop_addr=176, adsr1=255, adsr2=24518, fmod=0, lfo0_flags=lfo1_flags=0 (not a noise or LFO-modulated voice); second KON (PCSX cad 352): the same ADPCM sample re-keyed (start_addr=4256 unchanged) at raw_pitch=5137 with loop_addr 176→4288 and adsr2 24518→20102 — ruling out SPU register-state mismatch at attack.** — `[D·R] 2/3`
  - D: `spu_voice_events.jsonl` voice-21 rows both sides + `probe_keyon_per_voice` 2/2 KON paired, 2/2 KOFF paired, drift 0.0/0.0 ms (2026-05-21)
  - R: `smd-player/src/native/fft_spu_mixer_native.cpp` (per-voice event rows + WAV emission) + `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd` (KON commit) — validated by the `ice_no_music` orchestrator run (`smd-player/workspace/orchestrator/run_effect_iteration.py`)
  - src: `research/effect_sound/working_documents/ICE_V21_COS_DIST_PHASE_DRIFT_INVESTIGATION.md`
- **Ice's feds channel 1 (pool slot 4, chB — channel-offset table +0x36A8, ch[1]=0x0055) is the 31-byte channel at feds +0x55 (Inst=0x8B) and is the only sustained looped bytecode in ice: `Repeat(0x03)` @ +0x60 + `Coda(0x99)` @ +0x64 nested in an outer `EndBar(0x90)` channel loop dispatches 56 Notes over 562 cadences (~2.4 s ascending arpeggio, relkey cycle 0, 5, 10, 3, 8 via 0x94 Octave / 0x96 LowerOctave between Notes), and the second `Instrument(0x72)` @ +0x68 arms the second KON — long-playback duration is what makes v21 the canary for the phase-drift measurement artifact (voices with ~50 ms of audible content stay at cos_dist ~0.002).** — `[S·D·R] 3/3`
  - S: `ice_no_music.bin` feds channel 1 @ +0x55 hex dump + decode (pair 0 config=1: ch[0]=0x002D 40 B / ch[1]=0x0055 31 B), doc Appendix C
  - D: `probe_event_dispatch` v21 trace — `0x99` Coda density every 30 cadences from cad 381 through 562 (2026-05-21)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/opcodes/_table.gd` (0x90/0x98/0x99) + `runtime/shared/per_tick/post_walker_lookahead.gd` (0x90/0x99 lookahead) + `runtime/shared/channel_state.gd` (`loop_stack`) — validated by `smd-player/workspace/orchestrator/run_effect_iteration.py --session ice_no_music`
  - src: `research/effect_sound/working_documents/ICE_V21_COS_DIST_PHASE_DRIFT_INVESTIGATION.md`
- **The `cure_no_music` effect SMD dispatches pitch-bend opcode `0xD0` (set) 1× and `0xD1` (add) 21×; pitch bend is applied to the pitch-register value that becomes the sinc.** — `[R] 1/3`
  - R: `smd-player/addons/exmateria_sound/runtime/shared/opcodes/_table.gd` L148 (`0xD0` → `pitch_bend_set.gd`, `0xD1` → `pitch_bend_add.gd`) + `runtime/shared/per_tick/pitch_staging.gd` (bend accumulator feeds the staged SPU pitch write) — no named test for the cure dispatch counts themselves (`0xD1` AddPitchBend is validated as Effect Sound Parity Ladder position 6, session `cure_min07_plus_ba_db_b0_c4_d1_d4`)
  - src: `research/effect_sound/working_documents/MIXER_AUDIO_DIVERGENCE.md`

- **Re-pairing `probe_chan_pitch_state.pre_pitch_hi` by (slot_idx, cadence_index) instead of call_index leaves the audible haste slots 2/3/4/5 (voices 18/19/20/21) with real `pre_pitch_hi` divergence — 119/207, 122/208, 43/208, 108/208 rows (57.5%, 58.7%, 20.7%, 51.9%) — a real writer-path divergence, not a pairing artifact, left open for a writer-tracking probe; it is ~one-tenth of the 1556/1664 mis-paired call_index count.** — `[D·R] 2/3`
  - D: `probe_chan_pitch_state.pre_pitch_hi` re-paired by (slot_idx, cadence_index) instead of call_index, `haste` capture (2026-05-24) — per-slot divergent rows 119/122/43/108 vs 1556/1664 under the old call_index pairing
  - R: `smd-player/addons/exmateria_sound/runtime/shared/channel_state.gd::pre_pitch_acc_u32` (Godot-side field; zero-init with no music-engine residue seeding at the doc's date) + `runtime/shared/per_tick/advance_lfo.gd` (writer path) — divergence open at doc date; the `probe_chan_pitch_state` pair in `smd-player/workspace/orchestrator/probe_validation_manifest.py` exposes it
  - src: `research/effect_sound/working_documents/PROBE_PAIRING_ITERATION_ORDER_FIX.md`
- **On `reraise_no_music` voice_20's onset is ~2.6 s late on Godot — first audible sample at 115,628 vs 167 on PCSX (over 100k samples of silence at the start of the Godot trace) — while the waveform itself pairs at cos_dist 0.0094 once aligned; voice_20's `pcsx_onset_samples = 167` is essentially "first IRQ after KON", so matching it requires v20's start_addr to be written on the first KON pass rather than Godot's later commit (the doc's open attribution at its date).** — `[D·R] 2/3`
  - D: `audio_score.json` onset + cos_dist + `spu_voice_events.jsonl` first-audible-sample positions — `reraise_no_music` (2026-05-17)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/play_sound.gd` (first-KON walker start_addr commit) — open residual; the `reraise_no_music` orchestrator audio score (`smd-player/workspace/orchestrator/run_effect_iteration.py --session reraise_no_music`) still shows onset 115,628 vs 167
  - src: `research/effect_sound/working_documents/PROBE_PITCH_FORMULA_STAGES_INTRODUCED.md`
- **On `reraise_no_music` voice_21 Godot computes the vol formula bit-exactly with PCSX at all four stages — Godot's odd-indexed stage rows (1, 3, 5, 7) exactly match PCSX v21 rows 1–4 (env_sample 32330/32239/32148/32057, voice_gated 25005/24934/24864/24794, global_gated 12404/12369/12334/12300, pan_base 16384) — so the 50+ row diff on `probe_vol_inputs`/`probe_vol_lr_staging` is a row-count/iteration-set divergence (PCSX iterates 4 active channels per drain pass, v18–v21; Godot emits 2, two channels' `CHAN1_VOL_PRESTAGE` gate bit 0x100 of `channel_word_1` unset on Godot), not a math divergence, and the vol formula is ruled out as the source of voice_21's cos_dist ≈ 0.40 at the doc date.** — `[S·D·R] 3/3`
  - S: stage BPs 0x800171F4/0x8001720C/0x8001722C/0x80017248 in FUN_80017118 (vol-formula body 0x800171C4..0x80017310) — `project-assets/fft-rom/scus_disassembly.txt` (doc references)
  - D: `probe_vol_formula_stages` reraise_no_music capture (2026-05-17) — the v21 match table above; PCSX 592 rows/stage (148 IRQs × 4) vs Godot 588 (147 × 4, −1 IRQ, the same anchor-flip artifact as `probe_kon_koff_mask`)
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/play_sound.gd::_drain_prestage_all_slots` + `_emit_vol_formula_stage` — validated by the `probe_vol_formula_stages` pair (`smd-player/workspace/orchestrator/probe_validation_manifest.py`)
  - src: `research/effect_sound/working_documents/PROBE_VOL_FORMULA_STAGES_INTRODUCED.md`

- **After the `protect_no_music` 6-opcode fix (`ba9acddc`, 2026-05-15) the full-mix cos_dist regressed 0.0900 → 0.1551 even though both per-voice scores improved (voice 20 0.0851 → 0.0137, voice 21 0.0262 → 0.0059) and `cos_dist_phase` climbed too (0.6300 → 0.6815) — the fingerprint of inter-voice phase misalignment: two phase-offset voices summed produce destructive spectral interference that lifts even the phase-invariant cos_dist, and the missing per-tick chan_8a pan-LFO updates on voice 21 are the leading contributor because chan_8a feeds the pan formula at PC `0x80017214` in FUN_80017118 — closing the pan-LFO gap should close most of the mix gap.** — `[S·D] 2/3`
  - D: orchestrator `run_effect_iteration.py --session protect_no_music` full_mix + cos_dist_phase metrics, post-fix `ba9acddc` vs pre-fix `6810465c` (2026-05-15)
  - S: PC `0x80017214` (pan formula consuming the chan_8a accumulator) — `scus_disassembly.txt` (doc §5)
  - R: none — full_mix inter-voice phase metric not present in godot-learning (probed godot-learning, smd-player, fft-sound-driver; the metric lives in the workspace scorer `smd-player/workspace/orchestrator/score_audio.py`, measurement tooling rather than reimplementation)
  - src: `research/effect_sound/working_documents/PROTECT_RESIDUAL_VOL_INPUTS_DEFICIT.md`

- **On `reraise_no_music` voice 18 (pool slot 2 — the earliest binding of the 5 `pair_triggered` timeline events, sids 2/2/3/13/13) is a silent driver through the whole pre-audible window: PCSX emits 56 KOFFs on mask `0x40000` across cadences 10..412 (~10-cadence ≈ 42 ms retrigger cycles, matching on both sides from cadence 422 onward), all of them before the audible onset (PCSX sample 99620 ≈ 2.26 s vs Godot 107356 ≈ 2.43 s), and the audible portions pair almost identically on both sides (cos_dist 0.018, rms_spec_err 0.029) — making reraise the structural-parity stress case for the silent-driver retrigger KOFF path, not an audio-quality one.** — `[D·R] 2/3`
  - D: `reraise_no_music` last_run capture (2026-05-16): `probe_kon_koff_mask` PCSX 72 KON / 76 KOFF vs Godot 71 / 20, all 56 PCSX-only KOFFs on voice 18 mask `0x40000` at cadences 10..412; `audio_score.json` voice_18 onset + cos_dist; `godot.log` `[timeline]` `pair_triggered` lines
  - R: `smd-player/addons/exmateria_sound/runtime/effect_sound/pool.gd::voice_for_slot` (slot 2 → voice 18, `SPU_VOICE_BASE_DEFAULT = 16`) + `runtime/effect_sound_controller.gd` (`pair_triggered` timeline firing) — validated by the `reraise_no_music` orchestrator run (`smd-player/workspace/orchestrator/run_effect_iteration.py`; `probe_kon_koff_mask` 148/148 PAIR, voice_18 KOFF 57/57)
  - src: `research/effect_sound/working_documents/RERAISE_KON_KOFF_DURATION_DRAIN_GATE.md`

- **On `reraise_no_music`, voices 18+19 `vol_register` sit uniformly 25344/22272 ≈ 1.139× above PCSX (voice 18: 296/298 rows diff, ratio band 1.125–1.167 with no timing skew, cos_dist 0.0085; voice 19: 117/119 rows) — the orchestrator's `_derive_chan92_init` collapsed the four `diag_chan_92_writers.jsonl` engine-init rows (25344 ×2, 22272 ×2) into one scalar taken from the first row (25344, voice 20 — PCSX's init order is 20→21→18→19) and Godot applied that single value to all four voices; `probe_vol_inputs` shows `chan_92` as the only divergent input of the seven (the other six match exactly), so the seed mismatch is the entire diff.** — `[D·R] 2/3`
  - D: `probe_vol_register` / `probe_vol_inputs` / `diag_chan_92_writers.jsonl`, `reraise_no_music` capture (2026-05-16) — voice 18 cad-0 input row: PCSX chan_92 22272 vs Godot 25344, all other columns identical; voice 21 0/167 rows (correct seed by ordering accident)
  - R: the doc's `smd-player/workspace/orchestrator/run_effect_iteration.py::_derive_chan92_init` (first-row scalar + `--chan92=N`) and `play_sound.gd::_chan92_init_override` are no longer in the current tree (grep-verified 2026-08-21); the seed now flows per-sound-id from `smd-player/addons/exmateria_sound/runtime/feds_bank.gd::chan_92_for` via `play_sound.gd::_apply_chan_92_init` — `godot-learning/tests/EffectSoundCaptureTest.gd` drives the feds-bank path
  - src: `research/effect_sound/working_documents/RERAISE_VOICE_18_19_CHAN_92_PER_VOICE_OVERRIDE.md`

- **On `reraise_no_music` voice 19 (0xED pan-LFO subslot-2 arm, cb_idx 3 = triangle), Godot's pan output oscillates between 0–100% L/R extremes while PCSX holds a steady ~35%L/65%R — the triangle handler was verified correct in vol mode (modes 1 & 2 on cure_4 voice 19), so the divergence is specific to reraise voice 19's pan output and was unresolved at the doc date (candidates: `chan_8a` magnitude clamping, FFT's 0x100 depth ceiling at PC 0x80017538, 0xED p2-byte `dir_flags` init).** — `[D·R] 2/3`
  - D: `probe_vol_register` `reraise_no_music` (2026-05-16) — first 5 rows PCSX (8646,4752) (8420,4628) (8193,4503) (7967,4379) (7742,4255) vs Godot (11621,1068) (2601,10677) (11430,0) (8485,6400) (119,10751)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/per_tick/advance_lfo.gd` + `runtime/shared/helpers/lfo_step_calc.gd` (the doc's `dispatcher.gd:529–557` triangle handler, since refactored into these modules) — pan-side residual open, no validating test named in the doc
  - src: `research/effect_sound/working_documents/RERAISE_VOICE_18_19_CHAN_92_PER_VOICE_OVERRIDE.md`

- **On `reraise_no_music` voice 21, post-LFO-fix-stack, every sequencer-side probe is clean — `probe_vol_register` 0/167, `probe_pitch_register` 0/169, `probe_lfo_subslot0/1/2_state` match PCSX exactly (the only gap being the structural −40 inactive-channel rows, PCSX walks 8 channels per tick where Godot emits at slot end) — yet `cos_dist` holds at 0.426, localizing the residual to the C++ SPU GDExtension mixer stage (`smd-player/src/native/`: envelope decay, sample interpolation, or ADPCM block-boundary handling), consistent with the `protect_no_music` envelope-tail conclusion the doc cites.** — `[D·R] 2/3`
  - D: `probe_vol_register` / `probe_pitch_register` / `probe_lfo_subslot*_state` + `audio_score.json` voice_21 cos_dist 0.426, `reraise_no_music` (2026-05-16)
  - D: full control-surface re-pair on the re-rendered `reraise_no_music` (2026-05-17): all 11 per-voice probes pair row-for-row with 0 mismatches — `probe_pitch_register` 169/169, `probe_pitch_inputs` 169/169 (pitch_base {18413, 21485}, pitch_bend {0, 400}, pool_a2 {0}), `probe_vol_register` 167/167, sample start/repeat 2/2 (0x1010/48 both sides), `probe_adsr1_{high,mid,low}_register` 2/2 each, `probe_adsr2_low_register` 4/4, `probe_kon_koff_mask` (v21 bits) 5/5, `probe_keyon_per_voice` 2/2 — and the KEYON/KOFF sequencer events pair at the exact same cadence (KON 2/2, KOFF 3/3, drift 0 ms); voice_21 is the session's dominant outlier (0.4207 vs voice_18 0.0178 / voice_19 0.0005 / voice_20 0.0113; removing it lands full_mix at the ≤0.02 band, full_mix 0.1690)
  - R: `smd-player/src/native/` SPU mixer (GDExtension) — the suspected stage named by the doc; no validating test, residual open at doc date
  - src: `research/effect_sound/working_documents/RERAISE_VOICE_18_19_CHAN_92_PER_VOICE_OVERRIDE.md`
  - ⚠ SUPERSEDED (2026-08-21) by: `probe_lfo_subslot2_state` does not pair on reraise — Godot's 3 silent-driver channels carry all-zero mode-2 sub-slot state where PCSX runs active pan-LFOs on all 4 channels (cad=419: 4 active PCSX rows vs 1 Godot; 1252 vs 1216 mode-2 captures; 240/592 `probe_vol_inputs` `chan_8a` mismatches), so the 40-row gap is silent-driver LFO-state divergence, not only structural inactive-channel rows
  - src: `research/effect_sound/working_documents/RERAISE_VOICE_21_RENDERED_AUDIO_DIVERGENCE_WITH_BIT_IDENTICAL_REGISTER_WRITES.md`

- **On the 2026-05-16 `reraise_no_music` baseline capture, voice 18 fires 57 PCSX KEYONs at exactly Δ=10 cadences ([1, 11, 21, …, 734]; 39× Δ=10, 7× Δ=20 — a Note(duration=10) loop with brief interruptions) while Godot fires only 2 (the initial bind at cad 0 and a Rest-driven outlier at cad 452): voice_18 cos_dist 0.811 / cos_dist_phase 0.992 (near-total divergence), `probe_note_handler` PAIRs 77/77 on both sides yet `probe_keyon_per_voice` shows 77 vs 19 — the break is between note dispatch and KEYON commit; the root cause at doc date is that the Godot port had conflated FFT's two per-channel counters into the single `note_duration` field (channel_state.gd:102, drain-counter semantics) with no wrap-to-zero KON_ARM fire, so its KON_ARM sources (initial bind, Rest, idle drain) fire only ~2–3 times on a 10-tick loop with one Rest — voice 18 accounts for 55 of the 58 missing KEYONs; voices 19/20/21 miss 1 each (16 vs 15, 2 vs 1, 2 vs 1); full_mix cos_dist 0.365; the secondary probe FAILs (`probe_walker_flag_word_entry` 785/928, `probe_vol_register` 585/728, `probe_envelope_tail` 2504/2738) are downstream — no re-KEY means no envelope restart, so Godot's voices live longer.** — `[S·D·R] 3/3`
  - S: doc §4.2/§6 static comparison — the missing wrap fire at PC `0x800152d0`–`f8` (gate `0x800152c4`), while the Godot `per_channel_tick` port (decrement at dispatcher.gd:833, `== 1` release-prep at :879) matches the drain-counter semantics only
  - D: `probe_pairs.json` + `audio_score.json` from the baseline orchestrator run of `reraise_no_music` (2026-05-16)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/dispatcher.gd:441-464` (the `idle_timeout` drain under the `0x600` gate now fires the re-KEY; in-code comment cites PC 0x800152C8-F8) with the counters split into `note_duration` / `idle_timeout` fields (`channel_state.gd:120-121`) — validated by the `reraise_no_music` `probe_kon_koff_mask` 148/148 pairing (post-fix voice_18 KOFF 57/57, cos_dist 0.006)
  - src: `research/effect_sound/working_documents/RERAISE_VOICE_18_PERIODIC_KEYON_MISSING.md`
- **On `reraise_no_music` voice 21's onset is 194 ms later on Godot than on PCSX (0.472 s vs 0.278 s on the 5%-of-peak-sustained-for-64-samples criterion — ~40 cadences / ~8500 samples) even though the KEYON/KOFF sequencer events pair at the exact same cadence with 0 ms drift (KON 2/2, KOFF 3/3): the gap is sub-cadence, i.e. something between "KEYON written" and "first audible sample" differs at sample granularity.** — `[D] 1/3`
  - D: `audio_score.json` onset fields + `event_stats.py` KEYON/KOFF pairing (±2-cad tolerance, ~4.86 ms/cad), `reraise_no_music` re-render (2026-05-17)
  - R: none — open divergence at the doc's date; the doc's own hypothesis (§7.6) is to check whether Godot defers commitment of `voice.start_addr` + `KON_bit | voice` to a later sub-cadence than PCSX (the KEYON-side analogue of the KOFF deferral issue in `KEYON_COMMIT_DEFERRAL_PROBE_DEFICIT.md`) — no responsible stage identified
  - src: `research/effect_sound/working_documents/RERAISE_VOICE_21_RENDERED_AUDIO_DIVERGENCE_WITH_BIT_IDENTICAL_REGISTER_WRITES.md`
- **On reraise voice 21 the pitch-register stream contains no descending sweep on EITHER side: the first note alternates 8927↔8157 every ~50 cadences (~240 ms, ≈2 Hz; 84 + 84 rows) and the second note (KEYON at cadence 423) writes raw pitch 16314 exactly once — yet PCSX renders a perceptually descending pitch, so the perceived drop is not present in the register stream on either engine.** — `[D] 1/3`
  - D: `probe_pitch_register` histogram (8927:84, 8157:84, 16314:1) + trajectory sampled every 20 rows (cad 1 pitch 8927 → cad 51 8157 → cad 102 8927 … cad 403 8927), `reraise_no_music` (2026-05-17)
  - R: none — the alternation is a measured property of the FFT runtime's register stream; Godot mirrors the identical 169 rows (0 mismatches, per the control-surface point above), so there is no reimplementation behaviour beyond that mirror to cite
  - src: `research/effect_sound/working_documents/RERAISE_VOICE_21_RENDERED_AUDIO_DIVERGENCE_WITH_BIT_IDENTICAL_REGISTER_WRITES.md`
- **On reraise voice 21's second note (raw_pitch 16314, KEYON at cadence 423) PCSX renders a ~3800 Hz dominant with sidebands at 3512/3572/3670/3734/3878/3956/4054/4176 Hz, while Godot and a standalone Python simulation (same ADPCM bytes + gauss kernel + algorithm) both render a clean 4180 Hz — the expected `sinc = 16314 << 4 = 261024` (3.984× the 1051 Hz source): PCSX's dense trace holds `RawPitch` = 16314 with most-common reconstructed sinc 262114, but the effective loop period stretches to 52–55 output samples where 168 source samples / 3.984 should give 42.2, and 73 of 171 sampled sinc-per-step values sit below the expected 262114 (many near 142832 = 8927 << 4, the previous note's pitch value).** — `[D·R] 2/3`
  - D: `spu_voice_dense.jsonl` (debug_voice=21 per-output-sample capture: sample_index/decoded_sample/interp_sample/mixed_sample/sb_pos/spos/curr_addr) + per-voice WAV spectra, `reraise_no_music` post-bank-alignment (2026-05-17)
  - R: `smd-player/src/shared/fft_spu_pitch_runtime.cpp::fft_set_voice_pitch` (`sinc = raw_pitch << pitch_to_sinc_shift` after clamp) + `smd-player/src/native/fft_spu_mixer_native.cpp::set_voice_pitch` — validated by the `probe_pitch_register` pair (`smd-player/workspace/orchestrator/probe_validation_manifest.py`, bp 0x8001B628, tolerance ±2); the clean 4180 Hz Godot render is the deterministic output of this path at pitch 16314
  - src: `research/effect_sound/working_documents/RERAISE_VOICE_21_RENDERED_AUDIO_DIVERGENCE_WITH_BIT_IDENTICAL_REGISTER_WRITES.md`

- **On `protect_no_music`, two independent SPU implementations — the Mednafen replay and Godot's native SPU mixer — both diverge from live PCSX by similar margins (post-trim full_mix STFT cos_dist 0.1401 for Mednafen vs 0.1044 for Godot; Mednafen-to-PCSX sample diff: first divergence sample 823 (~18.6 ms in), 230,165/231,902 samples (99.2%) divergent, peak |error| 16,829, rms 2,228) — corroborating `project_protect_no_music_pcsx_borked.md` that it is PCSX's SPU render, not Godot's synth or the input stream, that is the outlier.** — `[D·R] 2/3`
  - D: `protect_no_music` orchestrator run (2026-05-15) — `audio_score_mednafen.json` full_mix cos_dist=0.1401 / phase=0.4410 / rms_spec_err=0.0503 vs `audio_score.json` cos_dist=0.1044 / phase=0.6564 / rms_spec_err=0.0727; post-trim sample diff `spu_mednafen.wav` vs `spu_mix.wav` (first divergence sample 823, 99.2% divergent, peak 16,829, rms 2,228)
  - R: `smd-player/src/native/fft_spu_mixer_native.cpp` (`FFTSpuMixerNative` — the Godot SPU mixer under test in the identical-input comparison) — validated by the orchestrator `score_audio` (`audio_score.json`) in `smd-player/workspace/orchestrator/run_effect_iteration.py`
  - src: `research/effect_sound/working_documents/SPU_EVENT_STREAM_REPLAY_PLAN.md`

- **The reraise voice 21 second-note divergence (the ~0.42 cos_dist residual that survived every clean per-voice register probe, and the ~3800 Hz PCSX vs 4180 Hz Godot peak) is SPU FMod pitch modulation, not a synth-stage defect: PCSX runs V21 with `Chan::FMod=1` throughout the capture (FMon_hi bit 21, re-asserted every SPU cadence by the per-IRQ mask writer), with V20 (FMod=2) as the silent frequency source — V20 emits a slowly-varying near-DC signal that averages slightly negative, so the per-sample `(32768 + iFMod)/32768` scale drags V21's effective sinc to ~91% of the bare `RawPitch << 4` (mean 237,060 vs 261,024 expected at raw_pitch 16314, ratio 0.908 — matching the 3800/4180 Hz peak ratio 0.909); arming the same V20/V21 pair in Godot's mixer collapses voice_21 cos_dist 0.418 → 0.0015 (cos_dist_phase 0.660 → 0.350) and full_mix 0.144 → 0.012, at a small cost of full_mix cos_dist_phase +0.09 because V20 is now (correctly) excluded from the master mix.** — `[S·D·R] 3/3`
  - S: `sh v1, 0x0(a0)` → FMon_hi `0x1F801D92` at PC `0x80019C8C` (generic register-pair helper, called from `FUN_8001B070` with a2=0xC8/a3=0xC9) — `scus_disassembly.txt`
  - D: `spu_voice_dense.jsonl` sinc reconstruction (V21 second note, samples 100,000–150,000: mean effective sinc 237,060 vs 261,024) + `spu_voice_21.wav` spectral peaks (3800 Hz modulated with sidebands vs 4180 Hz clean) + `FModOn` debug log (796 calls, val=0x0020 ×396) + post-fix `audio_score.json` — `reraise_no_music` capture (2026-05-17)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/opcodes/fmod_enable.gd` (0xB2) + `fmod_disable.gd` (0xB3) + `shared/flush_tick.gd:161-168` (`fmod_pending` → `set_voice_fmod(voice−1, 2)` + `set_voice_fmod(voice, 1)`, replacing the superseded per-session hardcode) + the iFMod chain in `smd-player/src/shared/fft_spu_mix_tools.cpp` — validated by the post-fix `reraise_no_music` orchestrator run (`smd-player/workspace/orchestrator/run_effect_iteration.py --session reraise_no_music`, 2026-05-17)
  - src: `research/effect_sound/working_documents/V21_FMOD_PITCH_MODULATION_FIX.md`

- **On `reraise_no_music` voice_19's session-top `rms_spec_err` (0.0338) is an L/R energy-distribution divergence, not amplitude: `mean_abs` pairs at PCSX 0.099 vs Godot 0.097 while `cos_dist` 0.1713 / `cos_dist_phase` 0.808 — the canonical L/R volume/modulation-drift fingerprint — driven by last-write-wins L/R volume registers: each drain pass the dispatcher iterates all 4 channels bound to a physical SPU voice and the last channel to commit `vol_l`/`vol_r` (the FUN_8001B41C / FUN_8001B4B0 / FUN_8001ACF0 write paths) wins, so PCSX's silent drivers (LFO-swept `chan_8a`) sweep the voice's pan tick-to-tick while Godot's silent drivers (pre-seed: `chan_8a` = 0 every cadence, static `vol_l`/`vol_r`) hold a near-constant split; the same mechanism compounds voice_21's chronic `cos_dist` 0.384, whose audible driver additionally runs a square-wave pitch-LFO (wf_idx=0) on top of the pan divergence.** — `[S·D] 2/3`
  - S: L/R volume write paths FUN_8001B41C / FUN_8001B4B0 / FUN_8001ACF0 — `project-assets/fft-rom/scus_disassembly.txt`
  - D: `audio_score.json` per-voice metrics, `reraise_no_music` (2026-05-17) — voice_19 `cos_dist` 0.1713 / `cos_dist_phase` 0.808 / `rms_spec_err` 0.0338, `mean_abs` 0.099/0.097; voice_21 `cos_dist` 0.3841; full_mix `rms_spec_err` 0.0349
  - R: `smd-player/addons/exmateria_sound/runtime/shared/note_handler/compute_vol_lr.gd` (per-channel `vol_l`/`vol_r` from `chan_8a`) + `runtime/shared/spu_irq_walker.gd::_fan_vol_lr_raw` (per-voice commit — the later channel overwrites the earlier one on the same SPU voice) fed by `runtime/effect_sound/play_sound.gd::_apply_lfo_state_seed` (silent-driver `chan_8a` seed) — validated by the `probe_vol_register` / `probe_vol_lr_staging` pairs + the `reraise_no_music` audio score (`smd-player/workspace/orchestrator/run_effect_iteration.py`)
  - src: `research/effect_sound/working_documents/VOICE_19_RMS_CHAN_8A_PAN_LFO_DIVERGENCE.md`
- **On `reraise_no_music` voice_19's first KON lands at SPU sample 810 on PCSX vs 34128 on Godot — a +33 k-sample (~0.77 s) shift that the orchestrator's activity-onset time-alignment keeps out of the audio metrics, but which remains a standalone parity gap the pan-LFO seed does not fix (flagged for separate investigation in the doc's §9.4).** — `[D] 1/3`
  - D: both sides' `spu_voice_events.jsonl` first-KON sample indices, `reraise_no_music` (2026-05-17)
  - R: none — no reimplementation of the reraise v19 first-KON offset present in godot-learning or smd-player (probed `godot-learning/src/`, `godot-learning/tests/`, `smd-player/addons/exmateria_sound/`; open at the doc's date)
  - src: `research/effect_sound/working_documents/VOICE_19_RMS_CHAN_8A_PAN_LFO_DIVERGENCE.md`

- **On `haste_no_music` voice 20's `env_vol` diverges on 201/408 rows (49%), all in `adsr_state=2` (Decay), after the lfo_handler relocation (commit 503c681a) eliminated the raw_pitch/fmod/pitch_register divergences: PCSX's env_vol drops fast then HOLDS at 6975 (0x1B3F) while Godot keeps decaying toward 0, repeating on every ~22-cadence re-key (max |Δ| 24896 = 75% of the 32767 envelope scale); voice 20 is the worst of the four haste voices (voice 18: 22%, voice 19: 6%, voice 20: 49%, voice 21: 14%).** — `[D·R] 2/3`
  - D: `probe_envelope_tail` env_vol pairing, `haste_no_music` post-relocation capture (2026-05-24) — 201/408 voice-20 rows, all `adsr_state=2` on both sides after the first cadence 79; pre-relocation the probe was 629/1570 divergent, post-relocation 372/1510 with 201 on voice 20
  - R: `smd-player/src/shared/fft_adsr_envelope.cpp` DECAY branch (per-mix-call `envelope_vol += kAdsrNumDec[rate]`, no 6975 hold — the Godot-side decay the probe exposes) — exposed by the `probe_envelope_tail` pair in `smd-player/workspace/orchestrator/run_effect_iteration.py --session haste_no_music`
  - src: `research/effect_sound/working_documents/VOICE_20_ENV_VOL_DECAY_SUSTAIN_DIVERGENCE.md`
- **Voice 20's chan-side ADSR writes are bit-identical PCSX↔Godot on `haste_no_music`: ADSR1 sustain-level nibble = 15, decay-shift nibble = 15, attack_rate = 0,0,35, mode_bit = 0 (write sites at cadences 1, 82, 323), and ADSR2 = {24518, 18694, 19974} (cadences 1, 82, 87, 323, 328) — localizing the env_vol divergence below the chan-side dispatch, into the SPU mixer's envelope evolution given identical register values.** — `[S·D·R] 3/3`
  - S: `0x8001B8B0` (`FUN_8001B8B0` `set_sustain_level`, writes ADSR1 bits 0–3), `0x8001ACF0` (KEY-ON / voice setup) — doc §9.2 disassembly references
  - D: `probe_adsr1_{low,mid,high}_register` + `probe_adsr2_register`, `haste_no_music` (2026-05-24)
  - R: `smd-player/addons/exmateria_sound/runtime/shared/spu_irq_walker.gd::_fan_adsr1_low/_mid/_high` (chan-side probe emit sites) + `runtime/spu.gd::set_voice_adsr1_low/_mid/_high` (L137–161) — validated by the `probe_adsr1_*`/`probe_adsr2_register` pairs
  - src: `research/effect_sound/working_documents/VOICE_20_ENV_VOL_DECAY_SUSTAIN_DIVERGENCE.md`
- **Voice 20's per-cadence env drop scales linearly with the Godot capture's `samples_per_sub` (~32 env per mix call: ~8384/cad at the CPU-lag-calibrated 262, ~5856/cad when pinned at canonical 183), and that per-call step matches the SUSTAIN rate (sustain_rate=36 → −32/call), not the DECAY rate (decay_rate=60 → −8 per 16 calls ≈ −0.5/call) — while Godot's `voice.adsr.state` still reports DECAY; pinning Godot at the canonical 183 samples/cadence did not shrink the divergence (208/407 vs 201/408 rows), so sample-rate scaling alone is not the whole story.** — `[D·R] 2/3`
  - D: `--pin-samples-per-sub` 183 vs 262 (CPU-lag calibrated) experiment on `haste_no_music` (2026-05-24)
  - R: `smd-player/workspace/harness/render_effect_sound.gd` (`--samples-per-sub` capture pacing; canonical 183 = 44100/240, per the orchestrator's `cadence_calibration.json` note that PRODUCTION uses 183) + `smd-player/src/shared/fft_adsr_envelope.cpp` (every rendered sample advances `envelope_vol_f`, decrementing `envelope_vol` on `kAdsrDenominator[rate]` crossing) — validated by the pin experiment above
  - src: `research/effect_sound/working_documents/VOICE_20_ENV_VOL_DECAY_SUSTAIN_DIVERGENCE.md`
- **A high-frequency PCSX trace (BP at the walker's per-slot loop entry `0x80014660`, firing ~24×/IRQ) shows voice 20 KEY-ONs every ~22 cadences (= 91 ms, the haste note interval), each cycle traversing Attack→Decay→6975→0 in 2–3 cadences with env_vol holding at 0 between cycles; between cadences 4–79 the env_vol held steady at 32767 for 75 cadences — a hold that no ADSR formula path produces given the confirmed-identical registers (sustain_level=15, sustain_rate=36, sustain_increase=0), so the SPU's mix() is NOT being called every sample for voice 20 on PCSX (the gating mechanism — ADPCM stop-flag, voice on/off gate, or a late ADSR1 rewrite — was unresolved at the doc's date).** — `[S·D] 2/3`
  - S: `0x80014660` (walker per-slot loop entry, the `diag_voice20_env_vol_trajectory.lua` BP site), `0x8001ACF0` (KEY-ON / voice setup) — doc §4.2/§9.2
  - D: `diag_voice20_env_vol_trajectory.lua` (BP @ `0x80014660`, env_vol transitions only), `haste_no_music` (2026-05-24)
  - R: none — the PCSX-side SPU mix gating is not present in godot-learning (probed godot-learning/src, godot-learning/tests); the Godot-side contrast — `voice.adsr.mix()` once per rendered sample in `smd-player/src/native/` (`fft_finalize_voice_mix_frame`) — is what makes Godot's env evolve continuously
  - src: `research/effect_sound/working_documents/VOICE_20_ENV_VOL_DECAY_SUSTAIN_DIVERGENCE.md`
- **On `haste_no_music` the per-IRQ `raw_pitch` error persists per-sample between staging IRQs, which is why voice 21's gap stays audible while `full_mix` passes: voice 21 stages `raw_pitch` only 186× in the 1664-IRQ capture window (~every 9 IRQs, ~11%), so a wrong staged value (the ~30 SPU-unit / quarter-tone median Δ) is held by the FM carrier across every output sample until the next staging IRQ, and FM converts that sub-percent pitch jitter into spectral sidebands — exactly what per-voice `cos_dist` measures; `full_mix` still lands at 0.011 because voices 18+19 are the dominant audible content and `score_audio.py`'s `_find_onset` + `_lag_align` absorb small pitch shifts as time shifts.** — `[D·R] 2/3`
  - D: `probe_pitch_register` / `probe_chan_pitch_state` voice-21 post-both-fixes rows, `haste_no_music` (2026-05-24) — 150/186 divergent staged rows, voice 21 cos_dist 0.427 vs full_mix 0.011
  - R: `smd-player/src/shared/fft_spu_mix_tools.cpp:173-207` iFMod chain (fmod==2 source feeds `i_fmod`; the fmod==1 carrier multiplies `effective_sinc` by `(32768 + i_fmod)/32768` per sample — mirrors PCSX-Redux `spu.cc:272-289` `FModChangeFrequency`) + `smd-player/addons/exmateria_sound/runtime/shared/per_tick/pitch_staging.gd` (the `raw_pitch` commit path whose value persists between commits) — no named test
  - src: `research/effect_sound/working_documents/VOICE_21_RAW_PITCH_DIVERGENCE_INVESTIGATION.md`

## Notes

(empty — user territory)

## Related

- [[Effect Sound Parity Ladder]]
- [[Battle Action SFX]]
- [[SPU Voice Engine]]
- [[WAVESET Instrument Bank]]
- [[KON KOFF Mask Dispatch]]
- [[Effect Sound Timing]]
- [[Effect Sound Slot Allocator]]
- [[LFO Sub-Slot Period Reset]]
- [[LFO Sub-Slot 2 Pan LFO]]
- [[LFO Callback Jumptable]]
- [[Dormant Slot Residue]]
- [[LFO Subslot Residue]]
- [[Noise LFO PRNG]]
- [[Ice V16 2-Cadence Pre-Arm]]
- [[PCSX-Redux Capture Rig]]
- [[FEDS Sound Definition Format]]
- [[SFX Index]]
- [[SPU Event Stream Replay]]
